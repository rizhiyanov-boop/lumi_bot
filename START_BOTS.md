# 🚀 Инструкция по запуску ботов

## ✅ Статус

Оба бота были запущены в фоновом режиме:
- ✅ **Мастер-бот** - процесс запущен
- ✅ **Клиент-бот** - процесс запущен

---

## 📋 Ручной запуск (если нужно перезапустить)

### Вариант 1: Через отдельные терминалы

**Терминал 1 - Мастер-бот:**
```bash
python run_master.py
```

**Терминал 2 - Клиент-бот:**
```bash
python run_client.py
```

### Вариант 2: Через PowerShell (фоновый режим)

```powershell
# Мастер-бот
Start-Process python -ArgumentList "run_master.py" -NoNewWindow

# Клиент-бот
Start-Process python -ArgumentList "run_client.py" -NoNewWindow
```

---

## ⚙️ Проверка конфигурации

Перед запуском убедитесь, что файл `.env` содержит:

```env
# Telegram Bot Tokens
BOT_TOKEN=ваш_токен_мастер_бота
CLIENT_BOT_TOKEN=ваш_токен_клиент_бота

# Database
DATABASE_URL=sqlite:///database.db

# Super Admins (comma-separated Telegram IDs)
SUPER_ADMINS=ваш_telegram_id
```

**Как получить токены:**
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Создайте первого бота для мастеров: `/newbot`
3. Создайте второго бота для клиентов: `/newbot`
4. Скопируйте токены в `.env`

---

## 📦 Установка зависимостей

Если боты не запускаются, установите зависимости:

```bash
pip install -r requirements.txt
```

Или через виртуальное окружение:

```bash
# Windows
venv\Scripts\activate
pip install -r requirements.txt

# Linux/Mac
source venv/bin/activate
pip install -r requirements.txt
```

**Зависимости:**
- python-telegram-bot==22.5
- sqlalchemy==2.0.44
- python-dotenv==1.0.0
- qrcode==8.2
- pillow==12.0.0

---

## 🔍 Проверка работы ботов

### Мастер-бот:
1. Откройте вашего мастер-бота в Telegram
2. Отправьте `/start`
3. Должно появиться приветственное сообщение

### Клиент-бот:
1. Откройте вашего клиент-бота в Telegram
2. Отправьте `/start`
3. Должно появиться сообщение с инструкциями

---

## 🛑 Остановка ботов

### Через диспетчер задач:
1. Откройте Диспетчер задач (Ctrl+Shift+Esc)
2. Найдите процессы `python.exe`
3. Завершите процессы, которые запускают `run_master.py` и `run_client.py`

### Через PowerShell:
```powershell
# Найти процессы
Get-Process python | Where-Object {$_.Path -like "*Lumi*"}

# Остановить процесс (замените PID на реальный ID процесса)
Stop-Process -Id <PID> -Force
```

---

## ❗ Устранение проблем

### Бот не запускается:
- ✅ Проверьте, что все зависимости установлены: `pip list`
- ✅ Проверьте наличие `.env` файла
- ✅ Проверьте, что токены в `.env` правильные
- ✅ Проверьте, что база данных создается (`database.db`)

### Ошибка импорта:
```bash
# Установите зависимости
pip install -r requirements.txt
```

### Ошибка токена:
- Проверьте файл `.env`
- Убедитесь, что токены правильные (без пробелов)
- Перезапустите бота

### База данных не создается:
- Проверьте права на запись в папку проекта
- Убедитесь, что SQLAlchemy установлен: `pip show sqlalchemy`

---

## 📝 Логи

Боты выводят логи в консоль. Для просмотра логов:

**Если запущены в фоновом режиме:**
- Логи не видны
- Запустите ботов в терминалах для просмотра логов

**Формат логов:**
```
[INFO] Инициализация базы данных...
[OK] База данных инициализирована!
[INFO] Запуск мастер-бота...
[OK] Мастер-бот успешно запущен!
```

---

**Дата создания:** 2025  
**Статус:** Боты запущены ✅

