# 🔧 Исправление проблемы с Python

## Проблема

Ошибка: `did not find executable at 'C:\Users\frold\AppData\Local\Programs\Python\Python314\python.exe'`

**Причина:** Виртуальное окружение `venv` было создано с Python 3.14, который больше не установлен на системе.

## ✅ Решение

### Вариант 1: Использовать системный Python (рекомендуется)

Скрипты уже исправлены для использования системного Python. Просто запустите:

```bash
# Установка зависимостей
install_dependencies.bat

# Или вручную:
python -m pip install -r requirements.txt
```

### Вариант 2: Пересоздать виртуальное окружение

Если хотите использовать venv:

1. **Удалите старый venv:**
   ```bash
   rmdir /s /q venv
   ```

2. **Создайте новый venv:**
   ```bash
   python -m venv venv
   ```

3. **Активируйте venv:**
   ```bash
   venv\Scripts\activate
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

## 📝 Что было исправлено

- ✅ `install_dependencies.bat` - теперь использует системный Python
- ✅ `start_master.bat` - теперь использует системный Python
- ✅ `start_client.bat` - теперь использует системный Python

## 🚀 Запуск после исправления

1. **Установите зависимости:**
   ```
   install_dependencies.bat
   ```

2. **Проверьте .env файл** (токены ботов должны быть указаны)

3. **Запустите боты:**
   - Окно 1: `start_master.bat`
   - Окно 2: `start_client.bat`

---

**Если проблемы остались**, попробуйте установить зависимости вручную:

```bash
python -m pip install python-telegram-bot==22.5 sqlalchemy==2.0.44 python-dotenv==1.0.0 qrcode==8.2 pillow==12.0.0
```

