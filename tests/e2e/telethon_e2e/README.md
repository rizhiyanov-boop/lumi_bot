# E2E-тестирование с Telethon

## 📋 Описание

Этот модуль содержит E2E (End-to-End) тесты для ботов Lumi с использованием библиотеки Telethon. Тесты взаимодействуют с реальными ботами через Telegram API, что позволяет проверять полные пользовательские сценарии.

## ⚡ Оптимизация и FloodWait

**ВАЖНО:** E2E тесты с Telethon используют реальный Telegram API и подвержены лимитам FloodWait.

### Реализованные оптимизации:
1. ✅ **Автоматическая обработка FloodWait** - тесты автоматически ждут при ограничениях
2. ✅ **Переиспользование мастера** - fixture `registered_master` экономит ~37% API запросов
3. ✅ **Маркеры тестов** - разделение на быстрые/медленные тесты
4. ✅ **Retry механизм** - автоматические повторные попытки при ошибках

**Подробнее:** См. [OPTIMIZATION_GUIDE.md](OPTIMIZATION_GUIDE.md)

### Рекомендуемая стратегия запуска:

```bash
# Для разработки (без E2E с Telethon)
pytest -m "not e2e_telethon"

# Перед коммитом (только быстрые E2E)
pytest -m "e2e_telethon and not slow"

# Полный прогон (раз в день)
pytest -m "e2e_telethon"
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements-test.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env.test` или добавьте в `.env`:

```env
# Telegram API (получить на https://my.telegram.org)
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=your_api_hash

# Тестовые боты (создать через @BotFather)
TEST_MASTER_BOT_TOKEN=your_test_master_bot_token
TEST_CLIENT_BOT_TOKEN=your_test_client_bot_token
TEST_MASTER_BOT_USERNAME=lumi_test_master_bot
TEST_CLIENT_BOT_USERNAME=lumi_test_client_bot

# Тестовая база данных (автоматически очищается перед каждым тестом)
TEST_DATABASE_URL=sqlite:///test_database_e2e.db
```

**⚠️ Важно:** Тестовые боты должны использовать ту же тестовую БД. При запуске тестовых ботов установите:
```env
DATABASE_URL=sqlite:///test_database_e2e.db
```

### 3. Создание тестовых ботов

1. Откройте @BotFather в Telegram
2. Создайте двух ботов:
   - `lumi_test_master_bot` (для тестирования мастер-бота)
   - `lumi_test_client_bot` (для тестирования клиент-бота)
3. Скопируйте токены в `.env.test`
4. Запустите тестовых ботов на отдельном сервере или локально

### 4. Получение API_ID и API_HASH

1. Перейдите на https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение и получите `api_id` и `api_hash`
5. Добавьте в `.env.test`

### 5. Запуск тестов

```bash
# Все E2E тесты с Telethon
pytest tests/e2e/telethon_e2e/ -v -m e2e_telethon

# Конкретный тест
pytest tests/e2e/telethon_e2e/test_master_registration.py -v

# С подробным выводом
pytest tests/e2e/telethon_e2e/ -v -s
```

## 📁 Структура

```
tests/e2e/telethon_e2e/
├── __init__.py
├── conftest.py              # Конфигурация pytest
├── fixtures.py              # Фикстуры для Telethon клиентов
├── helpers.py               # Вспомогательные функции
├── test_master_registration.py  # Тесты регистрации мастера
├── test_master_services.py      # Тесты добавления услуг
└── README.md                # Этот файл
```

## 🧪 Доступные тесты

### Регистрация мастера

- `test_master_registration_full_flow` - Полная регистрация с использованием имени из Telegram
- `test_master_registration_custom_name` - Регистрация с кастомным именем
- `test_master_registration_with_description` - Регистрация с описанием

### Услуги

- `test_add_service_full_flow` - Полное добавление услуги (категория → шаблон → название → цена → длительность)

## 🔧 Вспомогательные функции

### Отправка сообщений

```python
from tests.e2e.telethon_e2e.helpers import send_message_and_wait

messages = await send_message_and_wait(client, bot, "/start")
```

### Нажатие кнопок

```python
from tests.e2e.telethon_e2e.helpers import click_button, click_button_by_text

# По координатам
await click_button(client, message, row=0, col=0)

# По тексту
await click_button_by_text(client, message, "Добавить")
```

### Проверки

```python
from tests.e2e.telethon_e2e.helpers import assert_message_contains, assert_has_buttons

assert_message_contains(message, "Добро пожаловать")
assert_has_buttons(message, min_buttons=2)
```

## 🏙️ Тестовый город для E2E тестов

Для упрощения E2E тестов добавлен специальный обработчик тестового города.

**Использование в тестах:**
```python
# Когда бот запрашивает город, отправьте специальный текст:
await send_message_and_wait(client, bot, "тестовый123123123129543")
```

**Что происходит:**
- Бот автоматически создает тестовый город "Тестовый Город" в БД
- Устанавливает этот город для текущего мастера
- Переходит в главное меню
- Работает независимо от флагов `waiting_city_name` в `context.user_data`

**Параметры тестового города:**
- Название: "Тестовый Город"
- Координаты: Москва (55.7558, 37.6173)
- Страна: Россия
- Часовой пояс: Europe/Moscow

**Реализация:**
- Обработчик: `bot/handlers/master/menu.py::handle_test_city_input`
- Регистрация: `bot/main_master.py` (group=-2, высокий приоритет)

## ⚠️ Важные замечания

1. **Автоматическая очистка БД** - База данных **автоматически очищается перед каждым тестом**
2. **Отдельная тестовая БД** - Используется отдельная тестовая БД, основная БД не затрагивается
3. **Тестовые боты** - Тестовые боты должны использовать ту же тестовую БД (`TEST_DATABASE_URL`)
4. **Таймауты** - Используются разумные таймауты для ожидания ответов от ботов
5. **Стабильность** - Тесты могут быть нестабильными из-за задержек Telegram API
6. **Тестовый город** - Используйте `тестовый123123123129543` для автоматического создания города

Подробнее о настройке тестовой БД: см. [TEST_DATABASE_SETUP.md](TEST_DATABASE_SETUP.md)

## 🐛 Решение проблем

### Тесты пропускаются

Если тесты пропускаются с сообщением "TELEGRAM_API_ID и TELEGRAM_API_HASH не установлены", проверьте:
- Установлены ли переменные окружения
- Правильно ли указаны `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`

### Боты не отвечают

- Убедитесь, что тестовые боты запущены
- Проверьте правильность токенов в `.env.test`
- Проверьте правильность username ботов

### Ошибки авторизации

- Убедитесь, что `api_id` и `api_hash` правильные
- Попробуйте пересоздать сессию Telethon (удалите файл сессии)

## 📚 Дополнительные ресурсы

- [Telethon Documentation](https://docs.telethon.dev/)
- [Telethon Examples](https://github.com/LonamiWebs/Telethon/tree/master/telethon_examples)
- [Telegram Bot API](https://core.telegram.org/bots/api)

