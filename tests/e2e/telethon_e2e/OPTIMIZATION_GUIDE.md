# Руководство по оптимизации E2E тестов

## 🎯 Цель оптимизации

Минимизировать количество запросов к Telegram API для избежания FloodWait ошибок и ускорения тестов.

## 🚀 Реализованные оптимизации

### 1. Автоматическая обработка FloodWait

**Что сделано:**
- Добавлены функции `_send_message_with_retry()` и `_click_button_with_retry()` в `helpers.py`
- Автоматическое ожидание при возникновении FloodWait (до 10 минут)
- Повторные попытки отправки (до 3 раз)

**Использование:**
```python
# Все функции автоматически используют retry механизм:
await send_message_and_wait(client, bot, "Привет")  # Автоматический retry
await click_button(client, message, 0, 0)  # Автоматический retry
```

**Настройка:**
```python
# В helpers.py
MAX_FLOOD_WAIT_TIME = 600  # Максимальное время ожидания (секунды)
```

### 2. Переиспользование зарегистрированного мастера

**Проблема:** Каждый тест регистрировал нового мастера → много API запросов

**Решение:**
- Fixture `registered_master_session` (scope="session") - создает данные один раз
- Fixture `registered_master` (scope="function") - быстро регистрирует мастера для каждого теста
- База данных очищается перед каждым тестом (autouse fixture)

**Использование:**
```python
@pytest.mark.asyncio
@pytest.mark.e2e_telethon
async def test_my_feature(telethon_client, test_master_bot, registered_master):
    """Тест с уже зарегистрированным мастером"""
    assert registered_master is not None
    master_id = registered_master['id']
    
    # Ваш тест здесь - мастер уже зарегистрирован!
```

**Экономия:** ~10-15 API запросов на тест

### 3. Маркеры для разделения тестов

**Доступные маркеры:**
- `@pytest.mark.unit` - Юнит-тесты (быстрые, без внешних зависимостей)
- `@pytest.mark.integration` - Интеграционные тесты (с БД, без внешних API)
- `@pytest.mark.e2e` - E2E тесты (полный цикл)
- `@pytest.mark.e2e_telethon` - E2E тесты с реальным Telegram API
- `@pytest.mark.slow` - Медленные тесты
- `@pytest.mark.fast` - Быстрые тесты

**Использование:**
```bash
# Только быстрые тесты (для разработки)
pytest -m "not slow"

# Только E2E тесты с Telethon (перед коммитом)
pytest -m "e2e_telethon"

# Все тесты кроме E2E с Telethon (для CI)
pytest -m "not e2e_telethon"

# Только медленные тесты (раз в день)
pytest -m "slow"
```

### 4. Объединение проверок в один тест

**Плохо:**
```python
async def test_feature_1():
    await register_master()  # 10 API запросов
    # Проверка feature 1

async def test_feature_2():
    await register_master()  # 10 API запросов
    # Проверка feature 2
```

**Хорошо:**
```python
async def test_full_flow(registered_master):
    # Мастер уже зарегистрирован (0 дополнительных запросов)
    # Проверка feature 1
    # Проверка feature 2
    # Проверка feature 3
```

## 📊 Статистика оптимизации

### До оптимизации:
- Регистрация мастера: ~15 API запросов
- Тест добавления услуги: ~25 API запросов
- **Итого на 1 тест:** ~40 API запросов

### После оптимизации:
- Регистрация мастера (первый тест): ~15 API запросов
- Регистрация мастера (последующие тесты): ~15 API запросов (но с переиспользованием данных)
- Тест добавления услуги: ~10 API запросов
- **Итого на 1 тест:** ~25 API запросов
- **Экономия:** ~37% API запросов

### С автоматическим retry:
- FloodWait обрабатывается автоматически
- Тесты не падают при временных ограничениях
- Возможность запускать больше тестов подряд

## 🎨 Рекомендации по написанию тестов

### 1. Используйте fixture `registered_master`

```python
@pytest.mark.asyncio
@pytest.mark.e2e_telethon
@pytest.mark.slow  # Обязательно для E2E с Telethon!
async def test_my_feature(telethon_client, test_master_bot, registered_master):
    """Ваш тест"""
    # Мастер уже зарегистрирован
    pass
```

### 2. Группируйте связанные проверки

```python
@pytest.mark.asyncio
@pytest.mark.e2e_telethon
@pytest.mark.slow
async def test_service_management_flow(registered_master):
    """Полный флоу управления услугами"""
    # 1. Добавление услуги
    # 2. Редактирование услуги
    # 3. Удаление услуги
    # Все в одном тесте!
```

### 3. Используйте маркеры

```python
# Для частого запуска
@pytest.mark.fast
async def test_quick_check():
    pass

# Для редкого запуска
@pytest.mark.slow
@pytest.mark.e2e_telethon
async def test_full_scenario():
    pass
```

### 4. Добавляйте задержки между действиями

```python
await send_message_and_wait(client, bot, "Привет")
await asyncio.sleep(2)  # Даем боту время на обработку

await click_button(client, message, 0, 0)
await asyncio.sleep(2)  # Даем время на обновление
```

### 5. Обрабатывайте ошибки gracefully

```python
try:
    await send_message_and_wait(client, bot, "Привет")
except FloodWaitError as e:
    # Автоматически обработается retry механизмом
    # Но можно добавить дополнительную логику
    logger.warning(f"FloodWait: {e.seconds}s")
    raise
```

## 🔧 Настройка окружения для минимизации FloodWait

### 1. Используйте несколько тестовых аккаунтов

```env
# .env.test.account1
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=hash1
TELETHON_TEST_SESSION=session1

# .env.test.account2
TELEGRAM_API_ID=789012
TELEGRAM_API_HASH=hash2
TELETHON_TEST_SESSION=session2
```

### 2. Запускайте тесты с задержками

```bash
# Запуск с задержкой между тестами
pytest tests/e2e/telethon_e2e/ --dist loadscope --maxfail=1

# Или вручную с паузами
pytest tests/e2e/telethon_e2e/test_master_registration.py
sleep 60
pytest tests/e2e/telethon_e2e/test_master_services.py
```

### 3. Настройте CI/CD для редких запусков

```yaml
# .github/workflows/e2e-tests.yml
on:
  schedule:
    - cron: '0 2 * * *'  # Раз в день в 2:00
  workflow_dispatch:  # Ручной запуск
```

## 📈 Мониторинг использования API

### Логирование запросов

Все функции в `helpers.py` логируют свои действия:

```python
logger.info(f"Sending message to bot: '{message}'")
logger.warning(f"FloodWait: требуется подождать {wait_seconds}с")
logger.error(f"FloodWait слишком долгий: {wait_seconds}с")
```

### Просмотр логов

```bash
# Запуск с подробными логами
pytest tests/e2e/telethon_e2e/ -v -s --log-cli-level=INFO
```

## 🎯 Целевые показатели

### Для разработки:
- **Быстрые тесты (unit, integration):** < 1 секунды на тест
- **Запуск:** Каждый коммит

### Для CI/CD:
- **E2E тесты без Telethon:** < 5 секунд на тест
- **Запуск:** Каждый PR

### Для ночных прогонов:
- **E2E тесты с Telethon:** < 60 секунд на тест
- **Запуск:** Раз в день

## 🚨 Что делать при FloodWait

### Если тест упал с FloodWait:

1. **Подождите указанное время** (будет в ошибке)
2. **Используйте другой аккаунт** (если есть)
3. **Запустите тесты позже** (через час)
4. **Проверьте, не запущено ли что-то еще** (другие тесты, боты)

### Если FloodWait постоянный:

1. Увеличьте задержки между действиями
2. Уменьшите количество тестов в одном прогоне
3. Используйте больше тестовых аккаунтов
4. Запускайте тесты реже

## 📚 Дополнительные ресурсы

- [Telegram API Limits](https://core.telegram.org/api/obtaining_api_id#api-id)
- [Telethon Documentation](https://docs.telethon.dev/)
- [Pytest Best Practices](https://docs.pytest.org/en/stable/goodpractices.html)

