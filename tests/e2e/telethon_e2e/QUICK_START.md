# 🚀 Быстрый старт с оптимизированными E2E тестами

## Что изменилось?

### ✅ Автоматическая обработка FloodWait
Теперь тесты **автоматически ждут** при возникновении FloodWait и повторяют попытки.

### ✅ Переиспользование мастера
Используйте fixture `registered_master` вместо ручной регистрации в каждом тесте.

### ✅ Маркеры для управления запуском
Запускайте только нужные тесты с помощью маркеров.

## Как писать новые тесты?

### Шаблон оптимизированного теста:

```python
import pytest
import asyncio
from tests.e2e.telethon_e2e.helpers import (
    send_message_and_wait,
    click_button_by_text,
)

@pytest.mark.asyncio
@pytest.mark.e2e_telethon
@pytest.mark.slow  # Обязательно для E2E с Telethon!
async def test_my_feature(
    telethon_client,
    test_master_bot,
    registered_master  # Мастер уже зарегистрирован!
):
    """
    Описание теста
    """
    # Проверяем, что мастер зарегистрирован
    assert registered_master is not None
    
    # Ваш тест здесь
    # Мастер уже в главном меню!
    messages = await telethon_client.get_messages(test_master_bot, limit=1)
    
    # Взаимодействие с ботом
    await click_button_by_text(telethon_client, messages[0], "Услуги")
    await asyncio.sleep(2)
    
    # Проверки
    # ...
```

## Как запускать тесты?

### Для разработки (быстро):
```bash
# Без E2E с Telethon (только unit и integration)
pytest -m "not e2e_telethon"
```

### Перед коммитом (средне):
```bash
# Только быстрые E2E тесты
pytest -m "e2e_telethon and not slow"
```

### Полный прогон (медленно):
```bash
# Все E2E тесты с Telethon
pytest -m "e2e_telethon" -v
```

### Конкретный тест:
```bash
# Один тест
pytest tests/e2e/telethon_e2e/test_master_services.py::test_add_service_full_flow -v
```

## Что делать при FloodWait?

### Автоматическая обработка (уже работает):
- Тесты автоматически ждут до 10 минут
- Повторяют попытки до 3 раз
- Логируют информацию о ожидании

### Если FloodWait слишком долгий:
1. **Подождите** указанное время
2. **Используйте другой аккаунт** (создайте `.env.test.account2`)
3. **Запустите позже** (через час)

### Профилактика FloodWait:
```bash
# Запускайте тесты реже
# Используйте маркеры для фильтрации
# Добавляйте задержки между тестами

# Пример: запуск с задержкой
pytest tests/e2e/telethon_e2e/test_master_registration.py && sleep 60 && pytest tests/e2e/telethon_e2e/test_master_services.py
```

## Примеры использования

### Тест с зарегистрированным мастером:
```python
@pytest.mark.asyncio
@pytest.mark.e2e_telethon
@pytest.mark.slow
async def test_add_service(registered_master, telethon_client, test_master_bot):
    """Тест добавления услуги"""
    # Мастер уже зарегистрирован - сразу к делу!
    messages = await telethon_client.get_messages(test_master_bot, limit=1)
    await click_button_by_text(telethon_client, messages[0], "Услуги")
    # ...
```

### Тест без зарегистрированного мастера:
```python
@pytest.mark.asyncio
@pytest.mark.e2e_telethon
@pytest.mark.slow
async def test_registration(telethon_client, test_master_bot, clean_test_database):
    """Тест регистрации (не используем registered_master)"""
    await send_message_and_wait(telethon_client, test_master_bot, "/start")
    # ...
```

### Быстрый тест (без Telethon):
```python
@pytest.mark.asyncio
@pytest.mark.integration
@pytest.mark.fast
async def test_database_logic():
    """Быстрый тест логики БД"""
    from bot.database.db import get_session, create_master
    # ...
```

## Полезные команды

```bash
# Список всех тестов
pytest --collect-only

# Список тестов с маркером
pytest --collect-only -m "e2e_telethon"

# Запуск с подробными логами
pytest tests/e2e/telethon_e2e/ -v -s --log-cli-level=INFO

# Запуск с остановкой на первой ошибке
pytest tests/e2e/telethon_e2e/ -x

# Запуск последнего упавшего теста
pytest --lf

# Показать доступные маркеры
pytest --markers
```

## Структура файлов

```
tests/e2e/telethon_e2e/
├── README.md                    # Основная документация
├── QUICK_START.md              # Этот файл
├── OPTIMIZATION_GUIDE.md       # Подробное руководство по оптимизации
├── conftest.py                 # Конфигурация pytest
├── fixtures.py                 # Фикстуры (включая registered_master)
├── helpers.py                  # Вспомогательные функции (с FloodWait retry)
├── test_master_registration.py # Тесты регистрации
└── test_master_services.py     # Тесты услуг
```

## Дополнительные ресурсы

- **Полная документация:** [README.md](README.md)
- **Руководство по оптимизации:** [OPTIMIZATION_GUIDE.md](OPTIMIZATION_GUIDE.md)
- **Настройка тестовой БД:** [TEST_DATABASE_SETUP.md](TEST_DATABASE_SETUP.md)
- **Telethon Docs:** https://docs.telethon.dev/

## Чеклист для нового теста

- [ ] Добавлен маркер `@pytest.mark.e2e_telethon`
- [ ] Добавлен маркер `@pytest.mark.slow` (если использует Telegram API)
- [ ] Используется `registered_master` (если нужен зарегистрированный мастер)
- [ ] Добавлены задержки `await asyncio.sleep()` между действиями
- [ ] Тест документирован (docstring)
- [ ] Тест проверен локально
- [ ] FloodWait обрабатывается автоматически (ничего делать не нужно!)

---

**Готово!** Теперь вы можете писать оптимизированные E2E тесты без беспокойства о FloodWait! 🎉

