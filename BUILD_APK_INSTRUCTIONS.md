# 📱 Инструкция по сборке APK для тестирования

## ⚠️ Важно: Для сборки APK нужен Android Studio

Android Studio - это единственный надежный способ собрать APK. Он бесплатный и устанавливается за 10 минут.

## 🚀 Пошаговая инструкция

### Шаг 1: Установите Android Studio

1. Скачайте с https://developer.android.com/studio
2. Установите (включите опцию "Android SDK")
3. Запустите Android Studio

### Шаг 2: Откройте проект

1. **File → Open**
2. Выберите папку `android/` из проекта Lumi
3. Дождитесь синхронизации Gradle (5-10 минут первый раз)

### Шаг 3: Настройте API endpoint

Откройте файл:
```
android/app/src/main/java/com/lumi/beauty/di/AppModule.kt
```

Измените строку 35:
```kotlin
val baseUrl = "http://10.0.2.2:8000/" // Для эмулятора
// или для реального устройства:
val baseUrl = "http://192.168.1.XXX:8000/" // Замените XXX на ваш IP
```

Узнайте ваш IP через:
```powershell
ipconfig
```
Ищите "IPv4 Address" в разделе вашего сетевого адаптера.

### Шаг 4: Запустите REST API

В отдельном терминале:
```bash
cd api
python run_api.py
```

API должен работать на `http://localhost:8000`

### Шаг 5: Соберите APK

В Android Studio:
1. **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождитесь завершения (1-2 минуты)
3. Нажмите **"locate"** в уведомлении

### Шаг 6: Найдите APK

APK будет здесь:
```
android/app/build/outputs/apk/debug/app-debug.apk
```

## 📲 Установка на телефон

### Вариант 1: Через USB (ADB)
```bash
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

### Вариант 2: Вручную
1. Скопируйте `app-debug.apk` на телефон
2. Откройте файл на устройстве
3. Разрешите установку из неизвестных источников
4. Установите!

## ✅ Готово!

Теперь у вас есть рабочий APK для тестирования!

## 🔧 Альтернатива: Использовать готовый скрипт

После открытия проекта в Android Studio хотя бы раз, можно использовать:

```powershell
cd android
.\gradlew.bat assembleDebug
```

Но для первого раза обязательно нужно открыть в Android Studio для генерации Gradle wrapper.

## ⚠️ Если что-то не работает

1. **Gradle sync failed** → Проверьте интернет, перезапустите Android Studio
2. **SDK not found** → Установите Android SDK через SDK Manager
3. **Build failed** → Проверьте, что все зависимости загружены
4. **API не работает** → Убедитесь, что API запущен и доступен

## 📝 Примечание

Первый запуск Android Studio и синхронизация Gradle могут занять 10-15 минут.
Последующие сборки будут быстрее (1-2 минуты).

