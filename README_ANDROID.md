# Lumi Beauty Android App

Полнофункциональное Android приложение для клиентов Lumi Beauty, дублирующее весь функционал клиентского Telegram бота.

## 🎨 Особенности

- **Современный UI** - Material Design 3 с красивым оформлением
- **Jetpack Compose** - Декларативный UI фреймворк
- **REST API** - FastAPI бекенд для взаимодействия с базой данных
- **Офлайн режим** - Room для кэширования данных
- **Асинхронность** - Kotlin Coroutines для плавной работы

## 📱 Функционал

### Реализовано:
- ✅ Просмотр списка мастеров
- ✅ Детальная информация о мастере
- ✅ Просмотр услуг с портфолио
- ✅ Навигация между экранами
- ✅ Material Design 3 тема

### В разработке:
- 🔄 Система бронирования
- 🔄 Поиск мастеров по городам
- 🔄 Просмотр записей
- 🔄 Уведомления

## 🚀 Быстрый старт

### 1. Запуск REST API

```bash
# Установите зависимости
cd mobile_app/api
pip install -r requirements.txt

# Запустите API
python run_api.py
```

API будет доступен на `http://localhost:8000`

### 2. Настройка Android приложения

1. Откройте проект в Android Studio
2. Синхронизируйте Gradle зависимости
3. Настройте API endpoint в `app/src/main/java/com/lumi/beauty/di/AppModule.kt`
4. Запустите на эмуляторе или устройстве

### 3. Сборка APK

```bash
cd mobile_app/android
./gradlew assembleRelease
```

APK: `mobile_app/android/app/build/outputs/apk/release/app-release.apk`

## 📂 Структура проекта

```
Lumi/
├── mobile_app/            # 📱 Мобильное приложение
│   ├── api/               # REST API (FastAPI)
│   │   ├── main.py        # Основной файл API
│   │   └── requirements.txt # Зависимости API
│   └── android/          # Android приложение
│       ├── app/
│       │   └── src/main/
│       │       └── java/com/lumi/beauty/
│       │           ├── data/      # API, репозитории
│       │           ├── ui/         # Compose UI
│       │           └── di/         # Dependency Injection
│       └── build.gradle.kts
└── bot/                   # Существующий Telegram бот
```

## 🔧 Технологии

### Backend (API)
- FastAPI
- SQLAlchemy
- Pydantic

### Android
- Kotlin
- Jetpack Compose
- Material Design 3
- Retrofit
- Room
- Hilt
- Coroutines
- Coil (загрузка изображений)

## 📝 API Endpoints

- `GET /api/masters` - Список мастеров
- `GET /api/masters/{id}` - Детали мастера
- `GET /api/masters/{id}/services/{id}/time-slots` - Доступные слоты
- `POST /api/bookings` - Создать бронирование
- `GET /api/bookings` - Список записей
- `GET /api/cities` - Список городов
- `GET /api/cities/{id}/masters` - Мастера в городе

## 🔐 Аутентификация

В текущей версии используется упрощенная аутентификация через `user_id` в заголовке:
```
Authorization: Bearer {user_id}
```

Для продакшена рекомендуется JWT токены.

## 📄 Лицензия

См. основной README проекта.

