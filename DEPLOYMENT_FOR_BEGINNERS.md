# 🚀 Пошаговая инструкция по развертыванию ботов для начинающих

> **Эта инструкция написана для тех, кто никогда не работал с серверами.**  
> Каждый шаг описан максимально подробно с объяснениями.

## 🎯 Два способа развертывания

### Способ 1: Автоматический (рекомендуется) ⚡

Если вы хотите сэкономить время, используйте автоматический скрипт `deploy.sh`:

1. Подключитесь к серверу (см. Шаг 2 ниже)
2. Скачайте код: `git clone https://github.com/your-username/lumi_bot.git`
3. Перейдите в папку: `cd lumi_bot`
4. Запустите скрипт: `bash deploy.sh`
5. Следуйте инструкциям на экране
6. Заполните файл `.env` когда скрипт попросит

**Скрипт автоматически:**
- ✅ Установит все необходимые программы
- ✅ Настроит виртуальное окружение
- ✅ Установит зависимости
- ✅ Создаст systemd сервисы
- ✅ Настроит автозапуск

### Способ 2: Ручной (для обучения) 📚

Если вы хотите понять, что происходит на каждом шаге, следуйте этой инструкции пошагово.

---

## 📋 Что вам понадобится

1. **Сервер VPS** (заказать у провайдера):
   - ✅ CPU: 1 ядро
   - ✅ RAM: 1 GB
   - ✅ Диск: 80 GB HDD
   - ✅ ОС: Ubuntu 24.04 LTS
   - ✅ Виртуализация: KVM
   - ✅ Предустановленное ПО: **не установлено** (чистый сервер)

2. **Компьютер с Windows** (откуда будете подключаться)

3. **Данные от сервера** (получите после заказа на почту или в панели управления):
   - 🔑 IP-адрес сервера (например: `123.45.67.89`)
   - 👤 Логин (обычно `root` или имя пользователя)
   - 🔐 Пароль (или SSH ключ)

4. **Токены Telegram-ботов**:
   - Токен Master-бота (от @BotFather)
   - Токен Client-бота (от @BotFather)
   - Username Client-бота (без @)

5. **Ваш Telegram ID** (узнать через @userinfobot)

---

## 📱 Шаг 1: Установка программы для подключения к серверу

### Для Windows:

1. **Скачайте PuTTY** (бесплатная программа для подключения):
   - Перейдите на сайт: https://www.putty.org/
   - Скачайте `putty.exe` (или полный установщик)
   - Установите программу

2. **Альтернатива - Windows Terminal** (если у вас Windows 10/11):
   - Откройте Microsoft Store
   - Найдите "Windows Terminal"
   - Установите (это встроенная программа в новых Windows)

---

## 🔐 Шаг 2: Подключение к серверу

### Вариант А: Через PuTTY

1. **Откройте PuTTY**

2. **Заполните форму**:
   - **Host Name (or IP address)**: Вставьте IP-адрес вашего сервера
   - **Port**: `22` (обычно уже стоит)
   - **Connection type**: `SSH` (обычно уже выбрано)
   - Нажмите **Open**

3. **При первом подключении** появится окно безопасности:
   - Нажмите **Да** (Yes)
   - Это нормально, просто система спрашивает подтверждение

4. **Введите логин**:
   - Обычно `root` или имя пользователя, которое дал провайдер
   - Нажмите Enter

5. **Введите пароль**:
   - ⚠️ **ВАЖНО**: При вводе пароля символы НЕ отображаются (это нормально!)
   - Просто введите пароль и нажмите Enter
   - Если ошибка - попробуйте еще раз (возможно, была опечатка)

6. **Если все правильно**, вы увидите что-то вроде:
   ```
   root@your-server:~#
   ```
   Это означает, что вы успешно подключились! 🎉

### Вариант Б: Через Windows Terminal (или PowerShell)

1. **Откройте PowerShell** (нажмите Win+R, введите `powershell`, нажмите Enter)

2. **Введите команду**:
   ```bash
   ssh root@109.248.203.50
   ```
   Замените `ВАШ_IP_АДРЕС` на реальный IP вашего сервера

3. **При первом подключении** введите `yes` и нажмите Enter

4. **Введите пароль** (символы не отображаются - это нормально)

5. **Если все правильно**, вы увидите строку с `#` или `$`

---

## ✅ Шаг 3: Проверка подключения

После подключения выполните команду:

```bash
whoami
```

Должно показать `root` или ваше имя пользователя.

**Если видите ошибку** - проверьте:
- Правильно ли ввели IP-адрес
- Правильно ли ввели пароль
- Работает ли интернет на вашем компьютере

---

## 🛠️ Шаг 4: Обновление системы

Первым делом обновим систему. Это важно для безопасности.

**Выполните команды по очереди** (после каждой нажимайте Enter):

```bash
apt update
```

Подождите, пока команда выполнится (может занять 1-2 минуты).

Затем:

```bash
apt upgrade -y
```

Эта команда может занять 5-10 минут. Просто подождите.

**Что делают эти команды:**
- `apt update` - обновляет список доступных программ
- `apt upgrade -y` - устанавливает обновления безопасности (флаг `-y` означает "да" на все вопросы)

---

## 👤 Шаг 5: Создание пользователя (опционально, но рекомендуется)

Работать от root небезопасно. Создадим обычного пользователя:

```bash
adduser lumi
```

**Вас попросят ввести:**
1. **Пароль** - придумайте надежный пароль (запишите его!)
2. **Подтверждение пароля** - введите тот же пароль еще раз
3. **ФИО** - можно просто нажать Enter (пропустить)
4. **Номер комнаты** - нажмите Enter
5. **Рабочий телефон** - нажмите Enter
6. **Домашний телефон** - нажмите Enter
7. **Другое** - нажмите Enter
8. **Y/n** - введите `Y` и нажмите Enter (подтверждение)

Теперь добавим пользователя в группу администраторов:

```bash
usermod -aG sudo lumi
```

И переключимся на нового пользователя:

```bash
su - lumi
```

Теперь вы работаете от имени пользователя `lumi`.

---

## 📦 Шаг 6: Установка необходимых программ

Установим все необходимое одной командой:

```bash
sudo apt install -y git curl wget build-essential python3-pip python3-venv nginx
```

Эта команда установит:
- `git` - для скачивания кода с GitHub
- `curl` и `wget` - утилиты для загрузки файлов
- `build-essential` - инструменты для компиляции
- `python3-pip` - менеджер пакетов Python
- `python3-venv` - для создания виртуального окружения
- `nginx` - веб-сервер (для API, если понадобится)

**Подождите 2-5 минут**, пока все установится.

---

## 📥 Шаг 7: Скачивание кода ботов

Теперь скачаем код ваших ботов с GitHub.

**Выполните команды:**

```bash
cd ~
```

```bash
git clone https://github.com/rizhiyanov-boop/lumi_bot.git
```

⚠️ **ВАЖНО**: Замените `your-username` на реальное имя вашего репозитория на GitHub!

**Если не знаете ссылку на репозиторий:**
1. Откройте ваш репозиторий на GitHub в браузере
2. Нажмите зеленую кнопку **Code**
3. Скопируйте ссылку (она будет вида `https://github.com/username/repo.git`)
4. Вставьте в команду вместо `your-username/lumi_bot.git`

Перейдем в папку с кодом:

```bash
cd lumi_bot
```

---

## 🐍 Шаг 8: Создание виртуального окружения Python

Виртуальное окружение нужно, чтобы изолировать зависимости проекта.

```bash
python3 -m venv venv
```

Эта команда создаст папку `venv` с изолированным Python.

Активируем виртуальное окружение:

```bash
source venv/bin/activate
```

**После активации** вы увидите `(venv)` в начале строки:
```
(venv) lumi@server:~/lumi_bot$
```

Это означает, что виртуальное окружение активно.

---

## 📚 Шаг 9: Установка зависимостей

Теперь установим все необходимые библиотеки для ботов:

```bash
pip install --upgrade pip
```

```bash
pip install -r requirements.txt
```

**Это займет 3-5 минут.** Подождите, пока все установится.

Вы увидите много строк с `Installing...` и `Successfully installed...` - это нормально.

---

## 📁 Шаг 10: Создание папок для логов и бэкапов

Создадим папки, где будут храниться логи и резервные копии:

```bash
mkdir -p logs
mkdir -p backups
```

---

## ⚙️ Шаг 11: Настройка конфигурации (.env файл)

Теперь нужно создать файл с настройками ботов.

```bash
nano .env
```

Откроется текстовый редактор `nano`.

**Вставьте следующий текст** (нажмите правую кнопку мыши или Shift+Insert):

```env
# Telegram Bot Tokens
BOT_TOKEN=ваш_токен_мастер_бота
CLIENT_BOT_TOKEN=ваш_токен_клиентского_бота
CLIENT_BOT_USERNAME=имя_вашего_клиентского_бота

# Database
DATABASE_URL=sqlite:///database.db

# Super Admins (через запятую, без пробелов)
SUPER_ADMINS=ваш_telegram_id

# YooKassa Payment Configuration (если используете)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
YOOKASSA_TEST_MODE=true

# Premium subscription
PREMIUM_PRICE=299.00
PREMIUM_DURATION_DAYS=30

# OpenAI API (для генерации описаний)
OPENAI_API_KEY=ваш_ключ_openai
```

**Что нужно заполнить:**

1. **BOT_TOKEN** - токен Master-бота (от @BotFather)
2. **CLIENT_BOT_TOKEN** - токен Client-бота (от @BotFather)
3. **CLIENT_BOT_USERNAME** - username Client-бота (без @, например: `my_client_bot`)
4. **SUPER_ADMINS** - ваш Telegram ID (можно узнать через @userinfobot)
5. **OPENAI_API_KEY** - ключ OpenAI (если используете генерацию описаний)

**Как сохранить файл в nano:**
1. Нажмите `Ctrl + O` (сохранить)
2. Нажмите `Enter` (подтвердить имя файла)
3. Нажмите `Ctrl + X` (выйти из редактора)

**Защитим файл от посторонних:**

```bash
chmod 600 .env
```

---

## 🔧 Шаг 12: Настройка автозапуска ботов (systemd)

Теперь настроим, чтобы боты запускались автоматически при перезагрузке сервера.

### Создание сервиса для Master Bot

```bash
sudo nano /etc/systemd/system/lumi-master.service
```

Вставьте следующий текст:

```ini
[Unit]
Description=Lumi Master Bot
After=network.target

[Service]
Type=simple
User=lumi
Group=lumi
WorkingDirectory=/home/lumi/lumi_bot
Environment="PATH=/home/lumi/lumi_bot/venv/bin"
ExecStart=/home/lumi/lumi_bot/venv/bin/python /home/lumi/lumi_bot/run_master.py
Restart=always
RestartSec=10
StandardOutput=append:/home/lumi/lumi_bot/logs/master.log
StandardError=append:/home/lumi/lumi_bot/logs/master_error.log

[Install]
WantedBy=multi-user.target
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

### Создание сервиса для Client Bot

```bash
sudo nano /etc/systemd/system/lumi-client.service
```

Вставьте следующий текст:

```ini
[Unit]
Description=Lumi Client Bot
After=network.target

[Service]
Type=simple
User=lumi
Group=lumi
WorkingDirectory=/home/lumi/lumi_bot
Environment="PATH=/home/lumi/lumi_bot/venv/bin"
ExecStart=/home/lumi/lumi_bot/venv/bin/python /home/lumi/lumi_bot/run_client.py
Restart=always
RestartSec=10
StandardOutput=append:/home/lumi/lumi_bot/logs/client.log
StandardError=append:/home/lumi/lumi_bot/logs/client_error.log

[Install]
WantedBy=multi-user.target
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

---

## 🚀 Шаг 13: Запуск ботов

Теперь перезагрузим systemd и запустим боты:

```bash
sudo systemctl daemon-reload
```

Включим автозапуск:

```bash
sudo systemctl enable lumi-master.service
sudo systemctl enable lumi-client.service
```

Запустим боты:

```bash
sudo systemctl start lumi-master.service
sudo systemctl start lumi-client.service
```

---

## ✅ Шаг 14: Проверка, что боты работают

Проверим статус ботов:

```bash
sudo systemctl status lumi-master.service
```

Должно быть написано `active (running)` зеленым цветом.

Нажмите `q`, чтобы выйти из просмотра.

Проверим второй бот:

```bash
sudo systemctl status lumi-client.service
```

Тоже должно быть `active (running)`.

---

## 📋 Шаг 15: Просмотр логов (если что-то не работает)

Если боты не запустились, смотрим логи:

```bash
sudo journalctl -u lumi-master.service -n 50
```

Это покажет последние 50 строк логов Master-бота.

Для Client-бота:

```bash
sudo journalctl -u lumi-client.service -n 50
```

**Типичные ошибки:**

1. **"ModuleNotFoundError"** - не все зависимости установлены
   - Решение: `source venv/bin/activate && pip install -r requirements.txt`

2. **"FileNotFoundError: .env"** - файл .env не найден
   - Решение: проверьте, что файл `.env` находится в папке `lumi_bot`

3. **"Invalid token"** - неправильный токен бота
   - Решение: проверьте токены в файле `.env`

---

## 🎉 Готово! Боты запущены

Если все прошло успешно, ваши боты теперь работают!

### Как проверить, что боты работают:

1. Откройте Telegram
2. Найдите вашего Master-бота
3. Отправьте команду `/start`
4. Бот должен ответить!

То же самое с Client-ботом.

---

## 📝 Полезные команды на будущее

### Остановить ботов:
```bash
sudo systemctl stop lumi-master.service
sudo systemctl stop lumi-client.service
```

### Запустить ботов:
```bash
sudo systemctl start lumi-master.service
sudo systemctl start lumi-client.service
```

### Перезапустить ботов:
```bash
sudo systemctl restart lumi-master.service
sudo systemctl restart lumi-client.service
```

### Посмотреть логи в реальном времени:
```bash
sudo journalctl -u lumi-master.service -f
```
(Нажмите `Ctrl + C` чтобы выйти)

### Обновить ботов (после изменений в коде):
```bash
cd ~/lumi_bot
git pull
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart lumi-master.service
sudo systemctl restart lumi-client.service
```

---

## 🔄 Шаг 16: Настройка автоматического резервного копирования (опционально)

Создадим скрипт для резервного копирования:

```bash
nano ~/lumi_bot/backup.sh
```

Вставьте:

```bash
#!/bin/bash

BACKUP_DIR="$HOME/lumi_bot/backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/lumi_backup_$DATE.tar.gz"

# Создаем резервную копию базы данных и логов
tar -czf "$BACKUP_FILE" \
    "$HOME/lumi_bot/database.db" \
    "$HOME/lumi_bot/.env" \
    "$HOME/lumi_bot/logs" 2>/dev/null

# Удаляем старые бэкапы (старше 30 дней)
find "$BACKUP_DIR" -name "lumi_backup_*.tar.gz" -mtime +30 -delete

echo "Backup created: $BACKUP_FILE"
```

Сохраните и сделайте исполняемым:

```bash
chmod +x ~/lumi_bot/backup.sh
```

Настроим автоматическое резервное копирование каждый день в 3:00 ночи:

```bash
crontab -e
```

Если спросит редактор - выберите `nano` (нажмите `1` или `Enter`).

Вставьте в конец файла:

```
0 3 * * * $HOME/lumi_bot/backup.sh >> $HOME/lumi_bot/logs/backup.log 2>&1
```

Сохраните: `Ctrl + O`, `Enter`, `Ctrl + X`

---

## ✅ Чек-лист: Проверка после установки

Отметьте выполненные пункты:

- [ ] Сервер заказан и получены данные доступа
- [ ] Успешно подключился к серверу по SSH
- [ ] Система обновлена (`apt update && apt upgrade`)
- [ ] Установлены все необходимые программы
- [ ] Код ботов скачан с GitHub
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и заполнен токенами
- [ ] Systemd сервисы созданы для обоих ботов
- [ ] Боты запущены и работают (`systemctl status`)
- [ ] Master-бот отвечает в Telegram
- [ ] Client-бот отвечает в Telegram
- [ ] Настроено автоматическое резервное копирование

---

## 🆘 Решение проблем

### Проблема: Не могу подключиться к серверу

**Симптомы:**
- PuTTY показывает "Connection refused" или "Connection timed out"
- Не могу ввести пароль

**Решения:**
1. ✅ Проверьте, правильно ли ввели IP-адрес (без пробелов, только цифры и точки)
2. ✅ Проверьте, что сервер запущен (зайдите в панель управления провайдера)
3. ✅ Проверьте, что порт 22 открыт (обычно открыт по умолчанию)
4. ✅ Попробуйте перезагрузить сервер через панель провайдера
5. ✅ Проверьте, что ваш компьютер подключен к интернету
6. ✅ Попробуйте отключить файрвол на компьютере временно (для проверки)

**Если ничего не помогает:**
- Свяжитесь с поддержкой провайдера VPS
- Убедитесь, что сервер не заблокирован

### Проблема: "Permission denied"

**Симптомы:**
- При подключении появляется "Permission denied"
- Не могу выполнить команды

**Решения:**
1. ✅ Проверьте, правильно ли ввели пароль (символы не отображаются - это нормально!)
2. ✅ Убедитесь, что используете правильного пользователя:
   - Если заказали сервер с root доступом - используйте `root`
   - Если создали пользователя `lumi` - используйте `lumi`
3. ✅ Если работаете от пользователя `lumi`, используйте `sudo` перед командами, требующими прав администратора
4. ✅ Попробуйте ввести пароль еще раз (возможно, была опечатка)

### Проблема: Бот не отвечает в Telegram

**Симптомы:**
- Бот запущен, но не отвечает на команды
- В Telegram бот показывает "не в сети"

**Решения:**
1. ✅ Проверьте статус бота:
   ```bash
   sudo systemctl status lumi-master.service
   ```
   Должно быть `active (running)` зеленым цветом

2. ✅ Проверьте логи на ошибки:
   ```bash
   sudo journalctl -u lumi-master.service -n 50
   ```
   Ищите строки с `ERROR` или `Exception`

3. ✅ Проверьте, что токены в `.env` правильные:
   ```bash
   nano ~/lumi_bot/.env
   ```
   Убедитесь, что токены не имеют лишних пробелов

4. ✅ Перезапустите бота:
   ```bash
   sudo systemctl restart lumi-master.service
   ```

5. ✅ Проверьте, что файл `.env` существует и доступен:
   ```bash
   ls -la ~/lumi_bot/.env
   ```

### Проблема: "Command not found"

**Симптомы:**
- При выполнении команды появляется "command not found: python" или подобное

**Решения:**
1. ✅ Убедитесь, что виртуальное окружение активировано:
   ```bash
   source venv/bin/activate
   ```
   Должно появиться `(venv)` в начале строки

2. ✅ Проверьте, что вы в правильной папке:
   ```bash
   pwd
   ```
   Должно быть `/home/lumi/lumi_bot` или `/root/lumi_bot`

3. ✅ Переустановите зависимости:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

4. ✅ Проверьте, что Python установлен:
   ```bash
   python3 --version
   ```
   Должно показать версию Python 3.10 или выше

### Проблема: "ModuleNotFoundError"

**Симптомы:**
- При запуске бота появляется ошибка "ModuleNotFoundError: No module named 'XXX'"

**Решения:**
1. ✅ Убедитесь, что виртуальное окружение активировано
2. ✅ Установите все зависимости:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```
3. ✅ Проверьте, что файл `requirements.txt` существует:
   ```bash
   ls -la ~/lumi_bot/requirements.txt
   ```

### Проблема: Не хватает места на диске

**Симптомы:**
- Ошибки "No space left on device"
- Команды не выполняются

**Решения:**
1. ✅ Проверьте использование диска:
   ```bash
   df -h
   ```
   Посмотрите на столбец "Use%" - если больше 90%, нужно очистить место

2. ✅ Удалите старые логи:
   ```bash
   sudo journalctl --vacuum-time=7d
   ```
   Это оставит только логи за последние 7 дней

3. ✅ Удалите старые бэкапы (если есть):
   ```bash
   ls -lh ~/lumi_bot/backups/
   rm ~/lumi_bot/backups/lumi_backup_20240101_*.tar.gz
   ```
   (замените дату на старую)

4. ✅ Очистите кэш пакетов:
   ```bash
   sudo apt clean
   ```

### Проблема: Бот постоянно перезапускается

**Симптомы:**
- В статусе видно "failed" или бот постоянно перезапускается
- В логах видны ошибки

**Решения:**
1. ✅ Проверьте логи ошибок:
   ```bash
   sudo journalctl -u lumi-master.service -n 100 --no-pager
   ```

2. ✅ Проверьте файл `.env` - возможно, там ошибка в настройках

3. ✅ Проверьте права доступа к файлам:
   ```bash
   ls -la ~/lumi_bot/
   ```

4. ✅ Попробуйте запустить бота вручную для диагностики:
   ```bash
   cd ~/lumi_bot
   source venv/bin/activate
   python run_master.py
   ```
   Это покажет ошибки напрямую в консоли

---

## 📞 Получение помощи

Если что-то не работает:

1. **Проверьте логи** - там обычно есть информация об ошибке
2. **Проверьте статус сервисов** - `sudo systemctl status lumi-master.service`
3. **Перезапустите сервер** - иногда помогает
4. **Создайте issue на GitHub** - опишите проблему и приложите логи

---

## 🎓 Что дальше?

После успешного развертывания:

1. **Протестируйте ботов** - проверьте все функции
2. **Настройте мониторинг** - следите за логами
3. **Настройте резервное копирование** - защитите данные
4. **Изучите команды systemd** - для управления ботами
5. **Настройте домен** (если нужен API) - для мобильного приложения

---

**Удачи с развертыванием! 🚀**

Если что-то непонятно - перечитайте соответствующий шаг или обратитесь за помощью.

