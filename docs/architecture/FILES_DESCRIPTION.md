# 📁 Описание файлов проекта

Полное описание всех файлов и их назначения в проекте.

---

## 📚 Документация

### README.md
Главный файл документации. Содержит:
- Обзор проекта
- Основные возможности
- Быстрый старт
- Ссылки на детальную документацию

### QUICKSTART.md
Быстрый старт для новичков:
- Получение токена бота
- Настройка за 5 минут
- Первый запуск
- Проверка работы

### INSTALLATION.md
Подробная инструкция по установке:
- Требования к системе
- Пошаговая установка
- Настройка окружения
- Production deployment
- Устранение проблем

### COMMANDS.md
Справочник всех команд:
- Команды пользователя
- Команды администратора
- Системные команды
- SQL запросы
- Полезные скрипты

### PROJECT_OVERVIEW.md
Обзор проекта:
- Реализованный функционал
- Архитектура
- Технологический стек
- Особенности UX
- Планы развития

### DESIGN.md
Проектирование системы:
- Функциональные требования
- Архитектура базы данных
- Структура проекта
- Пользовательские сценарии
- Система ценообразования

### CHECKLIST.md
Чек-лист перед запуском:
- Проверка установки
- Проверка конфигурации
- Тестирование функций
- Проверка безопасности

### FILES_DESCRIPTION.md
Этот файл - описание всех файлов проекта.

---

## 🚀 Запуск

### run.py
Простой скрипт запуска бота:
```python
python run.py
```
Импортирует и запускает `bot.main.main()`

---

## 📦 Конфигурация

### requirements.txt
Список зависимостей Python:
- `python-telegram-bot==21.0.1` - Telegram Bot API
- `sqlalchemy==2.0.23` - ORM для БД
- `python-dotenv==1.0.0` - Загрузка .env

### .env.example
Пример файла конфигурации:
- `BOT_TOKEN` - токен Telegram бота
- `DATABASE_URL` - путь к базе данных
- `SUPER_ADMINS` - ID супер-администраторов
- `WORK_START` / `WORK_END` - рабочие часы

### .env (создается пользователем)
Рабочий файл конфигурации с реальными значениями.
**НЕ ПУБЛИКУЕТСЯ** (в .gitignore)

### .gitignore
Исключения для git:
- Python кэш (`__pycache__`)
- Виртуальное окружение (`venv/`)
- База данных (`*.db`)
- Конфигурация (`.env`)

---

## 🤖 Код бота

### bot/

Основная директория с кодом бота.

#### bot/__init__.py
Инициализация пакета `bot`.

#### bot/config.py
Конфигурация приложения:
- Загрузка переменных из `.env`
- Настройки рабочих часов
- Настройки бронирования (мин/макс продолжительность)
- Ценообразование (множители, скидки)

#### bot/main.py
**Точка входа** - главный файл запуска:
- Инициализация базы данных
- Создание приложения бота
- Регистрация всех обработчиков команд
- Регистрация callback обработчиков
- Запуск polling
- Обработка ошибок

---

### bot/database/

Работа с базой данных.

#### bot/database/__init__.py
Инициализация пакета `database`.

#### bot/database/models.py
**SQLAlchemy модели** БД:

**Модели:**
- `Location` - локации (пейнтбольные клубы)
- `Field` - площадки для игр
- `Booking` - бронирования
- `Admin` - администраторы
- `Settings` - системные настройки

**Enums:**
- `BookingStatus` - статусы бронирования (pending, confirmed, cancelled)

#### bot/database/db.py
**Функции работы с БД**:

**Инициализация:**
- `init_db()` - создание таблиц
- `create_test_data()` - создание тестовых данных
- `get_session()` - контекстный менеджер для сессий

**Локации:**
- `get_all_locations()` - все локации
- `get_location()` - одна локация по ID

**Площадки:**
- `get_fields_by_location()` - площадки локации
- `get_field()` - одна площадка по ID

**Бронирования:**
- `get_bookings_by_field_and_date()` - бронирования площадки на дату
- `get_user_bookings()` - бронирования пользователя
- `get_all_bookings()` - все бронирования (с фильтрами)
- `create_booking()` - создать бронирование
- `cancel_booking()` - отменить бронирование
- `get_booking()` - одно бронирование по ID

**Администраторы:**
- `is_admin()` - проверка прав админа
- `get_admin()` - получить админа
- `get_all_admins()` - все админы
- `add_admin()` - добавить админа
- `update_admin_notify()` - обновить настройки уведомлений

---

### bot/handlers/

Обработчики команд и действий пользователя.

#### bot/handlers/__init__.py
Инициализация пакета `handlers`.

#### bot/handlers/user.py
**Обработчики пользователя**:

**Команды:**
- `start_command()` - `/start` - приветствие и меню
- `help_command()` - `/help` - справка
- `my_bookings_command()` - `/my_bookings` - мои бронирования

**Callback обработчики:**
- `show_booking_details()` - детали бронирования
- `main_menu_callback()` - возврат в главное меню

#### bot/handlers/booking.py
**Логика процесса бронирования**:

**Процесс бронирования:**
- `start_booking()` - начало бронирования
- `select_location()` - выбор локации
- `select_field()` - выбор площадки
- `select_date()` - выбор даты (с навигацией по календарю)
- `select_time()` - выбор времени начала
- `select_duration()` - выбор продолжительности
- `receive_players_count()` - получение количества игроков
- `confirm_booking()` - подтверждение и создание

**Отмена:**
- `cancel_booking_process()` - отмена процесса бронирования
- `request_cancel_booking()` - запрос на отмену бронирования
- `confirm_cancel_booking()` - подтверждение отмены

**Состояния:**
- `WAITING_FOR_PLAYERS` - ожидание ввода количества игроков

#### bot/handlers/admin.py
**Админ-панель и функции**:

**Главная панель:**
- `admin_panel()` - показать админ-панель

**Бронирования:**
- `admin_all_bookings()` - все бронирования
- `admin_booking_details()` - детали (админская версия)
- `admin_bookings_page()` - пагинация

**Статистика:**
- `admin_stats()` - показать статистику

**Уведомления:**
- `admin_notify_on()` - включить уведомления
- `admin_notify_off()` - выключить уведомления

---

### bot/keyboards/

Inline клавиатуры для интерфейса.

#### bot/keyboards/__init__.py
Инициализация пакета `keyboards`.

#### bot/keyboards/inline.py
**Все inline клавиатуры**:

**Основные:**
- `get_main_menu_keyboard()` - главное меню
- `get_locations_keyboard()` - выбор локации
- `get_fields_keyboard()` - выбор площадки
- `get_date_keyboard()` - календарь выбора даты
- `get_time_keyboard()` - выбор времени
- `get_duration_keyboard()` - выбор продолжительности
- `get_confirm_keyboard()` - подтверждение бронирования

**Управление бронированиями:**
- `get_my_bookings_keyboard()` - список своих бронирований
- `get_booking_details_keyboard()` - детали бронирования
- `get_cancel_confirmation_keyboard()` - подтверждение отмены

**Админские:**
- `get_admin_panel_keyboard()` - админ-панель
- `get_admin_bookings_keyboard()` - список всех бронирований
- ... и другие админские клавиатуры

---

### bot/utils/

Утилиты и вспомогательные функции.

#### bot/utils/__init__.py
Инициализация пакета `utils`.

#### bot/utils/notifications.py
**Система уведомлений для админов**:

- `notify_admins_new_booking()` - уведомление о новом бронировании
  - Отправляет детали бронирования
  - Информацию о клиенте
  - Только админам с `notify=True`

- `notify_admins_cancellation()` - уведомление об отмене
  - Информация об отмененном бронировании
  - Кто отменил (пользователь или админ)

#### bot/utils/validators.py
**Валидация и расчеты**:

**Парсинг:**
- `parse_work_time()` - парсинг времени из строки
- `get_work_hours()` - получить рабочие часы

**Доступность:**
- `is_time_slot_available()` - проверка доступности слота
- `get_available_times()` - список занятых слотов
- `calculate_max_duration()` - макс. продолжительность от времени

**Расчет цены:**
- `calculate_price()` - расчет стоимости с учетом:
  - Базовой цены площадки
  - Множителя для выходных
  - Множителя для вечернего времени
  - Скидок за количество игроков

**Форматирование:**
- `format_booking_info()` - форматирование деталей бронирования
- `can_cancel_booking()` - проверка возможности отмены

---

## 🗄️ База данных

### database.db
SQLite база данных (создается автоматически при первом запуске).

**Таблицы:**
- `locations` - локации
- `fields` - площадки
- `bookings` - бронирования
- `admins` - администраторы
- `settings` - настройки

**НЕ ПУБЛИКУЕТСЯ** - содержит реальные данные.

Регулярно делайте бэкап!

---

## 📊 Статистика кода

**Всего файлов:** ~25

**Строк кода:**
- Python: ~2000+
- Документация (Markdown): ~3000+

**Основные компоненты:**
- 3 обработчика (user, booking, admin)
- 5 моделей БД
- 15+ inline клавиатур
- 20+ функций работы с БД

---

## 🔗 Связи между файлами

```
run.py
  └─> bot/main.py
       ├─> bot/config.py
       ├─> bot/database/db.py
       │    └─> bot/database/models.py
       ├─> bot/handlers/user.py
       ├─> bot/handlers/booking.py
       ├─> bot/handlers/admin.py
       │    └─> bot/keyboards/inline.py
       │    └─> bot/utils/notifications.py
       │    └─> bot/utils/validators.py
       └─> Telegram Bot API
```

---

## 💡 Как найти нужный код?

### Хочу изменить сообщения бота
→ `bot/handlers/user.py`, `bot/handlers/booking.py`

### Хочу изменить кнопки
→ `bot/keyboards/inline.py`

### Хочу изменить расчет цены
→ `bot/utils/validators.py` (функция `calculate_price`)

### Хочу изменить рабочие часы
→ `.env` (WORK_START, WORK_END)

### Хочу добавить новую локацию
→ SQL запрос в `database.db` или код в `bot/database/db.py`

### Хочу изменить структуру БД
→ `bot/database/models.py`

### Хочу добавить новую команду
→ 1) Создать handler в `bot/handlers/`
→ 2) Зарегистрировать в `bot/main.py`

### Хочу изменить уведомления
→ `bot/utils/notifications.py`

---

## 📖 Рекомендуемый порядок изучения

1. **README.md** - общий обзор
2. **QUICKSTART.md** - быстрый старт
3. **bot/main.py** - точка входа
4. **bot/database/models.py** - структура данных
5. **bot/handlers/user.py** - базовые команды
6. **bot/handlers/booking.py** - процесс бронирования
7. **bot/keyboards/inline.py** - интерфейс
8. **bot/utils/** - вспомогательные функции

---

Это полное описание всех файлов проекта. Удачи в разработке! 🚀

