# 🔗 Реализация Deep Links для разделения клиентов разных клубов

## 📋 Обзор

Реализована система Deep Links (глубоких ссылок), которая позволяет каждому клубу иметь уникальную ссылку для привлечения клиентов. При переходе по ссылке пользователь автоматически привязывается к соответствующему клубу и видит только его локации и площадки.

## 🎯 Проблема и решение

### Проблема
В одном боте могут быть клубы из одного города, и нужно разделять клиентов разных клубов, чтобы:
- Клиенты видели только локации своего клуба
- Владельцы управляли только своими данными
- Была возможность отслеживать источник клиентов

### Решение
Deep Links - уникальные ссылки для каждого клуба в формате:
```
https://t.me/БотUsername?start=SLUG_КЛУБА
```

## ✨ Как это работает

### 1. Структура ссылки

Каждый клуб имеет уникальный `slug` (идентификатор):

```
Клуб "Экстрим Москва"  → https://t.me/PaintballBot?start=extreme_moscow
Клуб "Арена СПб"       → https://t.me/PaintballBot?start=arena_spb
Клуб "Combat Казань"   → https://t.me/PaintballBot?start=combat_kazan
```

### 2. Процесс привязки пользователя

1. **Клиент переходит по ссылке** → `https://t.me/PaintballBot?start=extreme_moscow`
2. **Бот получает параметр** → `extreme_moscow`
3. **Бот находит клуб** в базе данных по `slug`
4. **Пользователь привязывается** → `context.user_data['club_id'] = club.id`
5. **Отображается приветствие** с названием клуба

### 3. Изоляция данных

После привязки пользователь видит:
- ✅ Только локации и площадки своего клуба
- ✅ Только доступные слоты своего клуба
- ✅ Только свои бронирования в этом клубе

Владелец клуба управляет:
- ✅ Только своими локациями
- ✅ Только своими инструкторами
- ✅ Только бронированиями своего клуба

## 🛠️ Реализация

### Файл: `bot/handlers/user.py`

#### Обновленная команда `/start`:

```python
async def start_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /start с поддержкой Deep Links"""
    user = update.effective_user
    
    # Получаем slug клуба из deep link (если передан)
    club_slug = None
    if context.args:
        club_slug = context.args[0]
    
    with get_session() as session:
        # Если передан slug - пытаемся найти клуб
        if club_slug:
            selected_club = session.query(Club).filter_by(slug=club_slug, active=True).first()
            if selected_club:
                # Привязываем пользователя к клубу
                context.user_data['club_id'] = selected_club.id
                context.user_data['club_slug'] = selected_club.slug
                club_name = selected_club.name
            else:
                # Slug не найден - используем дефолтный клуб
                context.user_data['club_id'] = 1
                club_name = None
        else:
            # Нет slug - используем последний выбранный или дефолтный
            club_id = context.user_data.get('club_id', 1)
            context.user_data['club_id'] = club_id
    
    # Персонализированное приветствие
    if club_name:
        welcome_text = f"👋 Привет, {user.first_name}!\n\nДобро пожаловать в <b>{club_name}</b>!"
    else:
        welcome_text = f"👋 Привет, {user.first_name}!\n\nДобро пожаловать в систему бронирования!"
```

### Файл: `bot/handlers/roles.py`

#### Панель суперадмина - отображение Deep Links:

```python
@require_superadmin
async def superadmin_clubs(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Получаем username бота
    bot_username = (await context.bot.get_me()).username
    
    # Для каждого клуба отображаем Deep Link
    for club in clubs_data:
        text += f"🔗 Deep Link: https://t.me/{bot_username}?start={club['slug']}\n\n"
```

#### Панель владельца клуба - персональная ссылка:

```python
@require_role(RoleType.CLUB_OWNER)
async def owner_panel(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Получаем информацию о клубе
    bot_username = (await context.bot.get_me()).username
    club = session.query(Club).get(club_id)
    
    # Формируем ссылку
    deep_link = f"https://t.me/{bot_username}?start={club.slug}"
    
    text = (
        f"👑 Панель владельца клуба\n\n"
        f"🏢 Клуб: <b>{club.name}</b>\n"
        f"🔗 <b>Ссылка для клиентов:</b>\n"
        f"<code>{deep_link}</code>\n\n"
        f"💡 Отправьте эту ссылку клиентам для бронирования!\n"
    )
```

## 📱 Где отображаются Deep Links

### 1. Суперадмин - раздел "Клубы"

```
🏢 Управление клубами

✅ Основной клуб (FREE)
   🆔 ID: 1 | Slug: default
   👤 Owner: Не назначен
   🔗 Deep Link: https://t.me/PaintballBot?start=default

✅ Экстрим Москва (BASIC)
   🆔 ID: 2 | Slug: extreme_moscow
   👤 Owner: 196256732
   🔗 Deep Link: https://t.me/PaintballBot?start=extreme_moscow
```

### 2. Владелец клуба - главная панель

```
👑 Панель владельца клуба

🏢 Клуб: Экстрим Москва
🆔 Club ID: 2
👤 User ID: 196256732
🔐 Ваши роли: club_owner

🔗 Ссылка для клиентов:
https://t.me/PaintballBot?start=extreme_moscow

💡 Отправьте эту ссылку клиентам для бронирования!
```

## 🎯 Примеры использования

### Создание нового клуба

1. Суперадмин создает клуб через панель:
   - Название: "Арена СПб"
   - Slug: `arena_spb`
   - План: BASIC

2. Система автоматически генерирует Deep Link:
   ```
   https://t.me/PaintballBot?start=arena_spb
   ```

3. Владелец клуба получает эту ссылку в своей панели

4. Владелец размещает ссылку:
   - На сайте клуба
   - В соцсетях (VK, Instagram)
   - В рекламных материалах
   - В Google/Yandex рекламе

### Клиент бронирует игру

1. **Переход по ссылке**:
   ```
   Клиент нажимает: https://t.me/PaintballBot?start=arena_spb
   ```

2. **Приветствие**:
   ```
   👋 Привет, Иван!
   
   Добро пожаловать в Арена СПб!
   
   🎯 Здесь вы можете забронировать игру...
   ```

3. **Бронирование**:
   - Клиент видит только локации "Арена СПб"
   - Выбирает площадку, дату, время
   - Подтверждает бронь

4. **Изоляция**:
   - Бронь привязана к клубу "Арена СПб" (club_id=2)
   - Владелец видит эту бронь в своей статистике
   - Другие клубы эту бронь НЕ видят

## 📊 Преимущества

### Для владельцев клубов:
- ✅ Уникальная ссылка для привлечения клиентов
- ✅ Отслеживание эффективности рекламных каналов
- ✅ Простота распространения (QR-коды, соцсети)
- ✅ Брендирование (клиент видит название клуба сразу)

### Для клиентов:
- ✅ Сразу попадают в нужный клуб
- ✅ Не видят чужие локации (меньше путаницы)
- ✅ Персонализированное приветствие

### Для системы:
- ✅ Полная изоляция данных между клубами
- ✅ Один бот для всех клубов (проще поддержка)
- ✅ Аналитика по источникам клиентов
- ✅ Масштабируемость (легко добавлять новые клубы)

## 🔒 Безопасность

### Изоляция данных:
- Все запросы к БД фильтруются по `club_id`
- Владелец клуба видит только свои данные
- Роли привязаны к конкретному клубу
- Нет доступа к данным других клубов

### Валидация slug:
- Только активные клубы (`active=True`)
- При неверном slug → дефолтный клуб
- Slug сохраняется в сессии пользователя

## 🚀 Будущие улучшения

### Фаза 1 (реализовано):
- ✅ Deep Links для каждого клуба
- ✅ Автоматическая привязка пользователей
- ✅ Отображение ссылок в панелях

### Фаза 2 (планируется):
- 🔜 Аналитика переходов по ссылкам
- 🔜 UTM-метки для отслеживания источников
- 🔜 QR-коды для оффлайн рекламы
- 🔜 Кастомные приветствия для каждого клуба

### Фаза 3 (в будущем):
- 🔜 Короткие ссылки (t.me/bot/arena вместо ?start=arena_spb)
- 🔜 Выделенные боты для Premium клиентов
- 🔜 Белый label (свой бренд бота)

## 📝 Проверка работоспособности

### Тест 1: Создание клуба
```bash
1. Суперадмин → /superadmin → Клубы → Создать клуб
2. Заполнить: название, slug, план
3. Проверить: Deep Link отображается
```

### Тест 2: Переход по ссылке
```bash
1. Скопировать Deep Link из панели
2. Открыть в браузере или отправить другому пользователю
3. Проверить: приветствие с названием клуба
4. Проверить: отображаются только локации этого клуба
```

### Тест 3: Изоляция данных
```bash
1. Создать 2 клуба с разными slug
2. Перейти по ссылке клуба А
3. Забронировать игру
4. Перейти по ссылке клуба Б
5. Проверить: бронь из клуба А не видна в клубе Б
```

---

**Дата реализации**: 28.10.2025  
**Статус**: ✅ Полностью реализовано и готово к использованию

