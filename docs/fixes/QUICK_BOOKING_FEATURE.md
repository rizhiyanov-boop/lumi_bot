# ⚡️ Быстрое бронирование для сотрудников

## 📋 Обзор

Реализована система для решения бизнес-проблемы: клубы используют несколько каналов бронирования (телефон, соцсети, личное обращение), и сотрудникам нужно быстро вносить такие бронирования в систему.

## ✨ Что реализовано

### 1. Новые поля в модели Booking

В таблицу `bookings` добавлены поля для отслеживания источника и создателя:

```python
phone = Column(String(20))  # телефон клиента
source = Column(String(50), default='telegram')  # telegram, phone, vk, instagram, staff, other
created_by = Column(Integer)  # ID сотрудника, создавшего бронь
```

### 2. Кнопка быстрого бронирования

- Добавлена кнопка **"⚡️ Быстрое бронирование"** в панели:
  - Владельца клуба (`/owner`)
  - Старшего инструктора (`/senior`)

### 3. Автоматическое отслеживание источника

Система автоматически определяет:

- **Обычные пользователи**: `source='telegram'`, `created_by=None`
- **Сотрудники (владельцы/старшие инструкторы)**: `source='staff'`, `created_by=<user_id>`

### 4. Расширенная функция create_booking

Функция `create_booking` в `bot/database/db.py` теперь принимает дополнительные параметры:

```python
def create_booking(
    # ... существующие параметры ...
    phone: str = None,
    source: str = 'telegram',
    created_by: int = None
) -> Booking:
```

## 🚀 Как использовать

### Для владельцев клуба и старших инструкторов:

1. Откройте панель управления (`/owner` или `/senior`)
2. Нажмите кнопку **"⚡️ Быстрое бронирование"**
3. Используйте стандартный процесс бронирования
4. Система автоматически пометит бронь как созданную сотрудником

### Для обычных пользователей:

Процесс бронирования не изменился - все работает как раньше, но теперь телефон сохраняется в отдельном поле.

## 📊 Аналитика

Теперь можно отслеживать:

- Какой сотрудник создал бронь (`created_by`)
- Из какого канала пришла бронь (`source`)
- Контактный телефон клиента (`phone`)

Это позволит в будущем:
- Анализировать эффективность каналов привлечения
- Отслеживать работу сотрудников
- Создавать отчеты по источникам бронирований

## 🔮 Будущие улучшения

В следующих версиях планируется:

1. **Упрощенный процесс для сотрудников**:
   - Выбор локации → Выбор площадки → Выбор даты и времени
   - Ввод имени клиента → Телефон (опционально)
   - Выбор источника (телефон/VK/Instagram/другое)
   - Быстрое подтверждение

2. **Шаблоны бронирования**:
   - Сохранение часто используемых настроек
   - Быстрое создание повторяющихся броней

3. **Массовое бронирование**:
   - Создание серии бронирований для корпоративных клиентов

## 🗂️ Измененные файлы

- `bot/database/models.py` - добавлены поля `phone`, `source`, `created_by`
- `bot/database/db.py` - расширена функция `create_booking`
- `bot/handlers/roles.py` - добавлена кнопка и обработчик `quick_booking_start`
- `bot/handlers/booking.py` - добавлено автоматическое определение источника
- `bot/main.py` - зарегистрирован новый обработчик

## 📝 Примеры использования в коде

### Создание обычного бронирования (клиентом):
```python
booking = create_booking(
    session=session,
    # ... параметры ...
    phone="+79991234567",
    source='telegram',  # автоматически
    created_by=None     # не сотрудник
)
```

### Создание бронирования сотрудником:
```python
booking = create_booking(
    session=session,
    # ... параметры ...
    phone="+79991234567",
    source='staff',           # сотрудник
    created_by=196256732      # ID сотрудника
)
```

### Бронь из другого канала (в будущем):
```python
booking = create_booking(
    session=session,
    # ... параметры ...
    phone="+79991234567",
    source='vk',              # из ВКонтакте
    created_by=196256732      # кто внес
)
```

---

**Дата реализации**: 28.10.2025  
**Статус**: ✅ Базовая функциональность готова

