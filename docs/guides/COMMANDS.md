# 📋 Справочник команд

## Команды для пользователей

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота и показать главное меню |
| `/book` | Начать процесс бронирования |
| `/my_bookings` | Показать мои бронирования |
| `/help` | Показать справку по использованию |

### Процесс бронирования

1. **Выбор локации** - нажмите на нужную локацию
2. **Выбор площадки** - выберите площадку (видно цену за час)
3. **Выбор даты** - выберите дату из календаря
4. **Выбор времени** - выберите время начала (🕐 - свободно, ❌ - занято)
5. **Продолжительность** - выберите длительность игры (1-4 часа)
6. **Количество игроков** - введите число в чат
7. **Подтверждение** - проверьте детали и подтвердите

## Команды для администраторов

### Админские команды (через кнопки)

Доступны через кнопку "👨‍💼 Админ-панель" в главном меню:

| Функция | Описание |
|---------|----------|
| 📊 Все бронирования | Просмотр всех бронирований |
| 📈 Статистика | Статистика за месяц и общая |
| 🔔 Уведомления ВКЛ | Включить уведомления о новых бронированиях |
| 🔕 Уведомления ВЫКЛ | Выключить уведомления |

### Управление бронированиями

- **Просмотр деталей** - нажмите на любое бронирование
- **Отмена** - доступна кнопка "🗑 Отменить бронирование"

## Системные команды (для разработчика)

### Локальный запуск

```bash
# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Запустить бота
python run.py
```

### Работа с зависимостями

```bash
# Установить зависимости
pip install -r requirements.txt

# Обновить зависимости
pip install --upgrade -r requirements.txt

# Добавить новую зависимость
pip install package_name
pip freeze > requirements.txt
```

### Работа с базой данных

```bash
# Открыть SQLite базу
sqlite3 database.db

# В SQLite:
.tables                    # Показать все таблицы
SELECT * FROM locations;   # Показать локации
SELECT * FROM bookings;    # Показать бронирования
.quit                      # Выход
```

### Очистка данных

```bash
# Удалить базу данных (ОСТОРОЖНО!)
# Windows:
del database.db

# Linux/Mac:
rm database.db

# При следующем запуске бота создастся новая БД с тестовыми данными
```

## Полезные SQL запросы

### Добавить новую локацию

```sql
INSERT INTO locations (name, address, description, active) 
VALUES ('Новая локация', 'Адрес', 'Описание', 1);
```

### Добавить новую площадку

```sql
INSERT INTO fields (location_id, name, description, capacity, price_per_hour, active) 
VALUES (1, 'Новая площадка', 'Описание', 25, 3500.0, 1);
```

### Просмотреть все активные бронирования

```sql
SELECT 
    b.id,
    b.date,
    b.start_time,
    b.user_name,
    f.name as field_name,
    l.name as location_name,
    b.total_price
FROM bookings b
JOIN fields f ON b.field_id = f.id
JOIN locations l ON f.location_id = l.id
WHERE b.status = 'confirmed'
ORDER BY b.date, b.start_time;
```

### Статистика по выручке

```sql
SELECT 
    strftime('%Y-%m', date) as month,
    COUNT(*) as bookings_count,
    SUM(total_price) as total_revenue,
    SUM(players_count) as total_players
FROM bookings
WHERE status = 'confirmed'
GROUP BY month
ORDER BY month DESC;
```

### Самые популярные площадки

```sql
SELECT 
    f.name,
    COUNT(*) as bookings_count,
    SUM(b.total_price) as total_revenue
FROM bookings b
JOIN fields f ON b.field_id = f.id
WHERE b.status = 'confirmed'
GROUP BY f.id
ORDER BY bookings_count DESC;
```

### Добавить нового админа

```sql
INSERT INTO admins (user_id, username, notify, is_super_admin) 
VALUES (123456789, 'username', 1, 0);
```

## Горячие клавиши в терминале

| Клавиши | Действие |
|---------|----------|
| `Ctrl + C` | Остановить бота |
| `↑` / `↓` | История команд |
| `Ctrl + L` | Очистить экран (Linux/Mac) |
| `cls` | Очистить экран (Windows) |

## Логи и отладка

### Включить подробное логирование

В `bot/main.py` измените:

```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.DEBUG  # Было: logging.INFO
)
```

### Просмотр логов в реальном времени

```bash
# Запустить бота с выводом в файл
python run.py > bot.log 2>&1

# В другом терминале:
# Linux/Mac:
tail -f bot.log

# Windows PowerShell:
Get-Content bot.log -Wait -Tail 50
```

## Переменные окружения (.env)

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен Telegram бота | `123:ABC...` |
| `DATABASE_URL` | URL базы данных | `sqlite:///database.db` |
| `SUPER_ADMINS` | ID супер-админов | `123,456,789` |
| `WORK_START` | Начало работы | `09:00` |
| `WORK_END` | Конец работы | `21:00` |

## Конфигурация в bot/config.py

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `MIN_DURATION` | Мин. продолжительность (ч) | 1 |
| `MAX_DURATION` | Макс. продолжительность (ч) | 4 |
| `WEEKEND_MULTIPLIER` | Множитель для выходных | 1.2 (+20%) |
| `EVENING_MULTIPLIER` | Множитель после 18:00 | 1.15 (+15%) |
| `EVENING_START_HOUR` | Начало вечера | 18 |
| `PLAYER_DISCOUNTS` | Скидки за игроков | см. config.py |

## Backup и восстановление

### Создать backup

```bash
# Windows
copy database.db database.backup.db

# Linux/Mac
cp database.db database.backup.db
```

### Восстановить из backup

```bash
# Остановите бота (Ctrl+C)

# Windows
copy database.backup.db database.db

# Linux/Mac
cp database.backup.db database.db

# Запустите бота снова
```

## Частые проблемы

### Бот не запускается

```bash
# Проверьте Python
python --version

# Проверьте виртуальное окружение
which python  # Linux/Mac
where python  # Windows

# Переустановите зависимости
pip install --force-reinstall -r requirements.txt
```

### База данных заблокирована

```bash
# Найдите процессы, использующие БД
# Linux:
lsof database.db

# Остановите бота и повторите попытку
```

### Уведомления не приходят админам

```sql
-- Проверьте настройки админа
SELECT * FROM admins WHERE user_id = YOUR_ID;

-- Включите уведомления
UPDATE admins SET notify = 1 WHERE user_id = YOUR_ID;
```

## Полезные ссылки

- [python-telegram-bot документация](https://docs.python-telegram-bot.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [@BotFather](https://t.me/botfather) - создание ботов
- [@userinfobot](https://t.me/userinfobot) - узнать свой ID

## Контакты для поддержки

При возникновении проблем:
1. Проверьте `INSTALLATION.md`
2. Посмотрите логи бота
3. Изучите код в `bot/` директории
4. Проверьте issues в репозитории

