# 🚀 Быстрый старт

## 1. Получите токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите название бота (например, "PaintballBooking")
   - Введите username бота (должен заканчиваться на "bot", например, "my_paintball_bot")
4. BotFather отправит вам токен. Скопируйте его!

## 2. Настройте бота

1. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте ваш токен:
```env
BOT_TOKEN=ваш_токен_здесь
SUPER_ADMINS=ваш_telegram_id
```

### Как узнать свой Telegram ID?

Отправьте любое сообщение боту [@userinfobot](https://t.me/userinfobot) - он пришлет ваш ID.

## 3. Установите зависимости

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## 4. Запустите бота

```bash
python run.py
```

или

```bash
python -m bot.main
```

## 5. Проверьте работу

1. Найдите вашего бота в Telegram по username
2. Отправьте `/start`
3. Попробуйте забронировать игру!

## Первое использование

При первом запуске бот автоматически:
- ✅ Создаст базу данных SQLite
- ✅ Создаст 2 тестовые локации
- ✅ Создаст 4 площадки
- ✅ Назначит вас администратором (если ваш ID в SUPER_ADMINS)

## Команды пользователя

- `/start` - Главное меню
- `/book` - Забронировать игру
- `/my_bookings` - Мои бронирования
- `/help` - Справка

## Команды администратора

После запуска бота, если ваш ID указан в `SUPER_ADMINS`, вам будут доступны:
- Админ-панель в главном меню
- Просмотр всех бронирований
- Статистика
- Управление уведомлениями

## Возможные проблемы

### Ошибка "BOT_TOKEN не установлен"
- Проверьте, что файл `.env` создан
- Проверьте, что в `.env` указан BOT_TOKEN

### Бот не отвечает
- Убедитесь, что бот запущен
- Проверьте, что токен правильный
- Проверьте консоль на наличие ошибок

### Нет прав администратора
- Убедитесь, что ваш Telegram ID указан в `SUPER_ADMINS` в `.env`
- Перезапустите бота

## Настройка рабочих часов и цен

Отредактируйте файл `bot/config.py` для изменения:
- Рабочих часов
- Множителей цен для выходных и вечера
- Скидок за количество игроков

## Backup базы данных

База данных хранится в файле `database.db`. Регулярно делайте его копии!

```bash
# Windows
copy database.db database.backup.db

# Linux/Mac
cp database.db database.backup.db
```

---

**Готово!** 🎉 Ваш бот готов к работе!

