# 👥 Руководство по работе с несколькими администраторами

## ✅ Да, система полностью поддерживает множественных администраторов!

### Как добавить нескольких админов

#### 1. Через файл `.env` (рекомендуется)

```bash
# Укажите несколько Telegram ID через запятую
SUPER_ADMINS=123456789,987654321,555666777

# Пример:
SUPER_ADMINS=196256732,111222333,444555666
```

**Как узнать Telegram ID:**
- Напишите боту [@userinfobot](https://t.me/userinfobot)
- Он сразу покажет ваш ID

#### 2. Через базу данных (для опытных)

```python
from bot.database.db import get_session, add_admin

with get_session() as session:
    admin = add_admin(
        session,
        user_id=123456789,
        username="username",
        first_name="Имя",
        is_super_admin=False  # True для супер-админа
    )
```

### Возможности каждого админа

#### Команды:
- `/admin` - открыть админ-панель
- `/all_bookings` - посмотреть все бронирования  
- `/stats` - статистика
- `/notify_on` - включить уведомления
- `/notify_off` - выключить уведомления

#### Функции:
- ✅ Просмотр всех бронирований
- ✅ Детальная информация о клиентах
- ✅ Отмена любых бронирований
- ✅ Статистика (доход, игроки, бронирования)
- ✅ Индивидуальные настройки уведомлений

### Типы администраторов

#### Супер-админы (`is_super_admin=True`)
- Автоматически создаются из `SUPER_ADMINS` в `.env`
- Могут управлять другими админами
- Все функции доступны

#### Обычные админы (`is_super_admin=False`)
- Добавляются вручную в БД
- Все стандартные функции доступны
- Разница с супер-админом - в будущих функциях управления

### Уведомления

#### Как это работает:
1. При новом бронировании уведомление получают **все админы** с `notify=True`
2. При отмене бронирования - аналогично
3. **Каждый админ** может индивидуально включить/выключить уведомления

#### Пример:
```
У вас 3 админа:
- Админ A: уведомления ВКЛ (/notify_on)
- Админ B: уведомления ВЫКЛ (/notify_off)  
- Админ C: уведомления ВКЛ (/notify_on)

Результат:
→ При новом бронировании уведомление получат только A и C
→ Админ B не будет получать уведомления (но все функции доступны)
```

### Проверка текущих админов

Создайте скрипт `check_admins.py`:

```python
from bot.database.db import get_session, get_all_admins

with get_session() as session:
    admins = get_all_admins(session)
    for admin in admins:
        status = "SUPER" if admin.is_super_admin else "ADMIN"
        notify = "ON" if admin.notify else "OFF"
        print(f"{status} | ID: {admin.user_id} | Notify: {notify}")
```

Запустите:
```bash
python check_admins.py
```

### Пример настройки для команды

#### Сценарий: Пейнтбольный клуб с 3 администраторами

**Файл `.env`:**
```bash
BOT_TOKEN=ваш_токен
SUPER_ADMINS=111111111,222222222,333333333
```

**Роли:**
- **Админ 1** (ID: 111111111) - Директор
  - Уведомления: ВКЛ
  - Проверяет все бронирования
  
- **Админ 2** (ID: 222222222) - Менеджер
  - Уведомления: ВКЛ
  - Обрабатывает новые заявки
  
- **Админ 3** (ID: 333333333) - Бухгалтер
  - Уведомления: ВЫКЛ (только статистика)
  - Смотрит только статистику и отчеты

### Безопасность

#### ⚠️ Важно:
- Не публикуйте файл `.env` в Git
- Не передавайте свой `BOT_TOKEN` третьим лицам
- Добавляйте в админы только проверенных людей
- Регулярно проверяйте список админов

#### Удаление админа:
```python
from bot.database.db import get_session, get_admin

with get_session() as session:
    admin = get_admin(session, user_id=123456789)
    if admin:
        session.delete(admin)
        session.commit()
```

### Часто задаваемые вопросы

**Q: Сколько админов можно добавить?**  
A: Неограниченное количество! Система рассчитана на работу с множественными администраторами.

**Q: Все админы видят одинаковую информацию?**  
A: Да, все админы имеют доступ ко всем бронированиям и статистике.

**Q: Можно ли разделить права админов?**  
A: Сейчас все админы имеют одинаковые права. Разделение прав можно добавить как будущую функцию.

**Q: Как добавить админа без перезапуска бота?**  
A: Добавьте его через БД (скрипт выше), бот автоматически распознает его при следующей команде.

**Q: Админ не получает уведомления. Почему?**  
A: Проверьте:
1. Админ добавлен в БД?
2. У админа `notify=True`?
3. Админ не заблокировал бота в Telegram?

### Резюме

✅ **Система полностью поддерживает множественных администраторов**  
✅ **Легко добавить через `.env`**  
✅ **Индивидуальные настройки уведомлений**  
✅ **Все админы равноправны**  
✅ **Масштабируемо для больших команд**

---

Нужна помощь? Проверьте:
- `README.md` - общая документация
- `QUICKSTART.md` - быстрый старт
- `INSTALLATION.md` - детальная установка


