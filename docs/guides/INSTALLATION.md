# 💻 Инструкция по установке и запуску

## Шаг 1: Подготовка окружения

### Требования
- Python 3.10 или выше
- pip (обычно устанавливается вместе с Python)
- Telegram аккаунт

### Проверка версии Python
```bash
python --version
```

Если версия ниже 3.10, обновите Python с [python.org](https://www.python.org/downloads/)

## Шаг 2: Создание Telegram бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте `/newbot`
4. Придумайте название: `Painball Booking Bot`
5. Придумайте username: `your_paintball_bot` (должен заканчиваться на `bot`)
6. **Скопируйте токен** - он выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 3: Настройка проекта

### 3.1 Создайте виртуальное окружение

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации вы увидите `(venv)` в начале строки терминала.

### 3.2 Установите зависимости

```bash
pip install -r requirements.txt
```

Должны установиться:
- python-telegram-bot
- sqlalchemy
- python-dotenv

### 3.3 Создайте файл .env

**Windows:**
```bash
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

### 3.4 Настройте .env файл

Откройте файл `.env` в текстовом редакторе и замените:

```env
BOT_TOKEN=ваш_токен_от_BotFather
SUPER_ADMINS=ваш_telegram_id
```

#### Как узнать свой Telegram ID?

1. Найдите в Telegram бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему `/start`
3. Он пришлет ваш ID (например, `123456789`)
4. Скопируйте этот ID в `.env` файл

Пример готового `.env`:
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
SUPER_ADMINS=987654321
DATABASE_URL=sqlite:///database.db
WORK_START=09:00
WORK_END=21:00
```

## Шаг 4: Запуск бота

### Первый запуск

```bash
python run.py
```

Вы должны увидеть:
```
🔧 Инициализация базы данных...
✅ База данных инициализирована
📝 Создание тестовых данных...
✅ Тестовые данные созданы
🤖 Запуск бота...
✅ Бот успешно запущен!
```

### Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Вы должны увидеть главное меню с кнопками

## Шаг 5: Проверка функционала

### Проверка пользовательских функций

1. Нажмите "🎯 Забронировать игру"
2. Выберите локацию
3. Выберите площадку
4. Выберите дату
5. Выберите время
6. Укажите продолжительность
7. Введите количество игроков (например, `10`)
8. Проверьте расчет цены
9. Подтвердите бронирование

### Проверка админских функций

Если ваш ID указан в SUPER_ADMINS:

1. В главном меню должна быть кнопка "👨‍💼 Админ-панель"
2. Нажмите на неё
3. Попробуйте "📊 Все бронирования"
4. Попробуйте "📈 Статистика"
5. Проверьте, что пришло уведомление о новом бронировании

## Устранение проблем

### Ошибка: "BOT_TOKEN не установлен"

**Причина:** Файл `.env` не создан или пуст

**Решение:**
1. Убедитесь, что файл `.env` существует в корне проекта
2. Откройте его и проверьте, что `BOT_TOKEN=...` заполнен
3. Перезапустите бота

### Ошибка: "No module named 'telegram'"

**Причина:** Зависимости не установлены

**Решение:**
```bash
pip install -r requirements.txt
```

### Бот не отвечает в Telegram

**Проверьте:**
1. Бот запущен и работает (не закрыли консоль?)
2. Токен правильный (скопировали полностью?)
3. В консоли нет ошибок
4. Бот не заблокирован вами в Telegram

### Нет кнопки "Админ-панель"

**Причина:** Ваш ID не в списке SUPER_ADMINS

**Решение:**
1. Узнайте свой ID через @userinfobot
2. Добавьте его в `.env`: `SUPER_ADMINS=ваш_id`
3. Перезапустите бота
4. Отправьте боту `/start` заново

### Ошибка при вводе количества игроков

**Проверьте:**
1. Вы вводите число, а не текст
2. Число больше 0
3. Число не превышает вместимость площадки

## Остановка бота

В терминале нажмите:
- **Windows:** `Ctrl + C`
- **Linux/Mac:** `Ctrl + C` или `Cmd + C`

## Повторный запуск

```bash
# Активируйте виртуальное окружение (если еще не активировано)
# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate

# Запустите бота
python run.py
```

## Настройка для production

### 1. Смените базу данных на PostgreSQL

В `.env`:
```env
DATABASE_URL=postgresql://user:password@localhost/paintball_db
```

Установите драйвер:
```bash
pip install psycopg2-binary
```

### 2. Используйте webhook вместо polling

Для deployment на серверах (Heroku, VPS) лучше использовать webhook. Подробности в документации [python-telegram-bot](https://docs.python-telegram-bot.org/).

### 3. Настройте systemd service (Linux)

Создайте файл `/etc/systemd/system/paintball-bot.service`:
```ini
[Unit]
Description=Paintball Telegram Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/painball
Environment="PATH=/path/to/painball/venv/bin"
ExecStart=/path/to/painball/venv/bin/python run.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable paintball-bot
sudo systemctl start paintball-bot
```

## Backup

### Backup базы данных

```bash
# Windows
copy database.db backups\database_%date%.db

# Linux/Mac
cp database.db backups/database_$(date +%Y%m%d).db
```

### Автоматический backup (Linux/Mac)

Добавьте в crontab (`crontab -e`):
```cron
0 3 * * * cp /path/to/painball/database.db /path/to/backups/database_$(date +\%Y\%m\%d).db
```

## Готово! 🎉

Ваш бот настроен и готов к работе!

Для получения помощи:
- Читайте `QUICKSTART.md`
- Смотрите `PROJECT_OVERVIEW.md`
- Изучите `DESIGN.md`

