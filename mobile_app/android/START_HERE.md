# 🚀 НАЧНИТЕ ЗДЕСЬ - Сборка APK

## ⚡ Быстрый способ получить APK (5-10 минут)

### Шаг 1: Установите Android Studio
1. Скачайте: https://developer.android.com/studio
2. Установите (включите "Android SDK")
3. Запустите

### Шаг 2: Откройте проект
```
File → Open → выберите папку android/
```
Дождитесь синхронизации (5-10 минут первый раз)

### Шаг 3: Соберите APK
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

### Шаг 4: Готово!
APK: `android/app/build/outputs/apk/debug/app-debug.apk`

---

## ⚙️ Перед сборкой (важно!)

### 1. Настройте API endpoint

Откройте: `android/app/src/main/java/com/lumi/beauty/di/AppModule.kt`

Измените строку 35:
```kotlin
val baseUrl = "http://10.0.2.2:8000/" // Для эмулятора
// или
val baseUrl = "http://ВАШ_IP:8000/" // Для реального устройства
```

Узнайте IP: `ipconfig` → IPv4 Address

### 2. Запустите REST API

```bash
cd api
python run_api.py
```

---

## 📱 Установка APK

Скопируйте `app-debug.apk` на телефон и установите!

---

**Примечание:** Без Android Studio собрать APK невозможно. Это стандартный инструмент разработки Android (бесплатный, ~1GB).

