# ⚡ Быстрый старт - Сборка APK за 5 минут

## 🎯 Самый простой способ

### 1. Установите Android Studio
- Скачайте: https://developer.android.com/studio
- Установите (включите Android SDK)

### 2. Откройте проект
```
Android Studio → File → Open → выберите папку android/
```

### 3. Дождитесь синхронизации
- Первый раз займет 5-10 минут (загрузка зависимостей)
- Дождитесь "Gradle sync finished"

### 4. Соберите APK
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

### 5. Готово! 
APK будет в: `android/app/build/outputs/apk/debug/app-debug.apk`

## 📱 Установка на телефон

1. Скопируйте APK на телефон
2. Откройте файл
3. Разрешите установку из неизвестных источников
4. Установите!

## ⚙️ Настройка API (важно!)

Перед сборкой откройте:
`android/app/src/main/java/com/lumi/beauty/di/AppModule.kt`

Измените `baseUrl`:
- Для эмулятора: `http://10.0.2.2:8000/`
- Для реального устройства: `http://ВАШ_IP:8000/` (узнайте через `ipconfig`)

## 🚀 Запуск API

В отдельном терминале:
```bash
cd api
python run_api.py
```

API должен работать на `http://localhost:8000`

## ✅ Готово к тестированию!

После сборки у вас будет рабочий APK, который можно установить и протестировать!

