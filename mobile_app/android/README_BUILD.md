# 🎯 Сборка APK - Пошаговая инструкция

## ⚡ Быстрый способ (через Android Studio)

### 1. Установите Android Studio
- Скачайте с https://developer.android.com/studio
- Установите с Android SDK

### 2. Откройте проект
```
File → Open → выберите папку android/
```

### 3. Дождитесь синхронизации
- Gradle загрузит все зависимости (5-10 минут первый раз)
- Дождитесь сообщения "Gradle sync finished"

### 4. Соберите APK
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

### 5. Найдите APK
После сборки нажмите **"locate"** в уведомлении или найдите:
```
android/app/build/outputs/apk/debug/app-debug.apk
```

## 🔧 Настройка перед сборкой

### 1. Настройте API endpoint

Откройте `app/src/main/java/com/lumi/beauty/di/AppModule.kt`:

```kotlin
val baseUrl = "http://10.0.2.2:8000/" // Для эмулятора
// или
val baseUrl = "http://192.168.1.100:8000/" // Для реального устройства (замените на ваш IP)
```

### 2. Запустите REST API

```bash
cd api
python run_api.py
```

API должен быть доступен на `http://localhost:8000`

## 📱 Установка на устройство

### Вариант 1: Через ADB
```bash
adb install app-debug.apk
```

### Вариант 2: Вручную
1. Скопируйте APK на телефон
2. Откройте файл на устройстве
3. Разрешите установку из неизвестных источников

## ⚠️ Решение проблем

### Ошибка: "Gradle sync failed"
- Проверьте интернет соединение
- Убедитесь, что Android SDK установлен
- Попробуйте **File → Invalidate Caches / Restart**

### Ошибка: "SDK not found"
- **File → Settings → Appearance & Behavior → System Settings → Android SDK**
- Установите Android SDK Platform 34

### Ошибка при сборке
- Убедитесь, что JDK 17+ установлен
- Проверьте, что все зависимости загружены
- Попробуйте **Build → Clean Project**, затем **Build → Rebuild Project**

## ✅ Готово!

После успешной сборки у вас будет APK файл, готовый к установке и тестированию!

