# 📁 Структура проекта Lumi Beauty

**Версия:** 2.0 (после очистки)  
**Обновлено:** 2025

---

## 🌳 Дерево файлов

```
Lumi/
│
├── 📄 README.md                    # Главная страница проекта
├── 📋 PROJECT_STRUCTURE.md         # Этот файл
├── 📊 PROJECT_ANALYSIS.md          # Анализ проекта
├── 🔧 requirements.txt             # Зависимости Python
├── ⚙️ .gitignore                   # Игнорируемые файлы Git
│
├── 🚀 run_master.py                # Запуск мастер-бота
├── 🚀 run_client.py                # Запуск клиент-бота
├── 🦇 start_bot.bat                # Windows launcher (если нужен)
│
├── 📚 docs/                        # Документация (старая - для архива)
│
├── 🗄️ database.db                  # SQLite база данных
│
├── 🤖 bot/                         # Основной код бота
│   ├── __init__.py
│   ├── main_master.py              # Entry point мастер-бота
│   ├── main_client.py              # Entry point клиент-бота
│   ├── config.py                   # Конфигурация
│   │
│   ├── 📊 database/                # Работа с БД
│   │   ├── __init__.py
│   │   ├── models.py               # SQLAlchemy модели (MasterAccount, Service, Booking, etc.)
│   │   └── db.py                   # CRUD операции
│   │
│   ├── 🎮 handlers/                # Обработчики команд
│   │   ├── __init__.py
│   │   ├── master.py               # Обработчики мастер-бота
│   │   └── client.py               # Обработчики клиент-бота
│   │
│   └── 🛠️ utils/                   # Утилиты
│       ├── __init__.py
│       ├── booking_utils.py        # Утилиты бронирования
│       ├── cache.py                # Кеширование
│       ├── debounce.py             # Дебаунс
│       ├── notifications.py        # Уведомления
│       ├── performance.py          # Производительность
│       └── validators.py           # Валидация данных
│
├── 📦 tests/                       # Тесты (старые - нужно переписать)
│
└── 📦 venv/                        # Виртуальное окружение (игнорируется)
```

---

## 📦 Ключевые модули

### 🤖 `bot/main_master.py` и `bot/main_client.py` - Точки входа

**Назначение:** Инициализация и запуск двух отдельных ботов

**Файлы:**
- `main_master.py` - Бот для мастеров
  - Регистрация мастеров
  - Управление профилем, услугами, расписанием
  - Генерация QR-кодов и deep links
  
- `main_client.py` - Бот для клиентов
  - Добавление мастеров по deep link
  - Просмотр услуг и мастеров
  - Бронирование услуг

**Запуск:**
```bash
python run_master.py  # Мастер-бот
python run_client.py  # Клиент-бот
```

---

### 📊 `bot/database/` - Слой данных

**Назначение:** Работа с базой данных

**Файлы:**
- `models.py` - SQLAlchemy модели
  - `MasterAccount` - Аккаунт мастера
  - `ServiceCategory` - Категории услуг
  - `Service` - Услуги
  - `WorkPeriod` - Расписание работы
  - `User` - Клиенты
  - `UserMaster` - Связь клиент ↔ мастер
  - `Booking` - Записи/бронирования
  
- `db.py` - CRUD операции
  - `get_session()` - Контекст менеджер для сессий
  - `init_db()` - Инициализация БД
  - `create_*()` / `get_*()` функции для всех моделей

**Пример использования:**
```python
from bot.database.db import get_session
from bot.database.models import MasterAccount

with get_session() as session:
    master = session.query(MasterAccount).filter_by(telegram_id=123456).first()
```

---

### 🎮 `bot/handlers/` - Обработчики

**Назначение:** Обработка команд и callback'ов

**Структура:**
```
handlers/
├── master.py       # Обработчики мастер-бота
└── client.py       # Обработчики клиент-бота
```

#### `handlers/master.py` - Мастер-бот

**Основные функции:**
- `start_master()` - Регистрация и главное меню
- `master_profile()` - Управление профилем
- `master_services()` - Управление услугами и категориями
- `master_schedule()` - Настройка расписания
- `master_qr()` - Генерация QR-кода и deep link
- `master_bookings()` - Просмотр записей

**Состояния ConversationHandler:**
- `WAITING_NAME` - Ожидание имени
- `WAITING_DESCRIPTION` - Ожидание описания

#### `handlers/client.py` - Клиент-бот

**Основные функции:**
- `start_client()` - Главное меню и обработка deep links
- `client_masters()` - Список мастеров
- `view_master()` - Просмотр профиля мастера
- `book_master()` - Процесс бронирования
- `client_bookings()` - История записей
- `remove_master_confirm()` - Удаление мастера из списка

---

### 🛠️ `bot/utils/` - Утилиты

**Назначение:** Вспомогательные функции

**Файлы:**
- `validators.py` - Валидация данных
- `booking_utils.py` - Утилиты для бронирования
- `cache.py` - Кеширование данных
- `debounce.py` - Дебаунс для callback'ов
- `notifications.py` - Отправка уведомлений
- `performance.py` - Мониторинг производительности

---

## 🔄 Поток обработки

### Регистрация мастера

```
/start (мастер-бот)
  └── start_master()
      └── create_master_account()
          └── MasterAccount создан
```

### Добавление мастера клиентом

```
Deep link: /start master_<TELEGRAM_ID> (клиент-бот)
  └── start_client()
      └── add_user_master_link()
          └── UserMaster создан
```

### Бронирование

```
"Записаться" → book_master()
  └── Выбор услуги
      └── Выбор даты/времени
          └── create_booking()
              └── Booking создан
```

---

## 📊 Зависимости модулей

```
main_master.py / main_client.py
    ├── handlers/master.py / handlers/client.py
    │   ├── database/db.py
    │   │   └── database/models.py
    │   └── utils/* (при необходимости)
    └── config.py
```

---

## 🚀 Расширение проекта

### Добавить новую функцию в мастер-бот:

1. Добавить функцию в `bot/handlers/master.py`
2. Зарегистрировать handler в `bot/main_master.py`
3. При необходимости добавить CRUD функцию в `bot/database/db.py`

### Добавить новую функцию в клиент-бот:

1. Добавить функцию в `bot/handlers/client.py`
2. Зарегистрировать handler в `bot/main_client.py`
3. При необходимости добавить CRUD функцию в `bot/database/db.py`

### Добавить новую модель:

1. Создать модель в `bot/database/models.py`
2. Добавить CRUD функции в `bot/database/db.py`
3. Обновить `init_db()` (автоматически через `Base.metadata.create_all()`)

---

## 📝 Соглашения по коду

### Именование:
- **Функции:** `snake_case`
- **Классы:** `PascalCase`
- **Константы:** `UPPER_CASE`

### Структура файла:
```python
"""Docstring модуля"""

# Импорты стандартной библиотеки
from datetime import datetime

# Импорты сторонних библиотек
from telegram import Update

# Локальные импорты
from bot.database.models import MasterAccount
from bot.database.db import get_session
```

---

## 🎓 Для новых разработчиков

### Изучи в таком порядке:
1. `README.md` - Общий обзор
2. `ARCHITECTURE.md` - Архитектура проекта
3. `bot/database/models.py` - Модели данных
4. `bot/handlers/master.py` - Примеры handlers мастер-бота
5. `bot/handlers/client.py` - Примеры handlers клиент-бота

### Полезные файлы для старта:
- `bot/main_master.py` - Точка входа мастер-бота
- `bot/main_client.py` - Точка входа клиент-бота
- `bot/database/db.py` - CRUD операции
- `bot/config.py` - Конфигурация

---

## 🔍 Что было удалено

В процессе очистки были удалены следующие файлы (старый проект paintball/lasertag):

- ❌ `bot/main.py` - старый entry point
- ❌ `bot/handlers/booking.py` - старое бронирование
- ❌ `bot/handlers/calculator.py` - калькулятор стоимости игр
- ❌ `bot/handlers/user.py` - пользовательские команды
- ❌ `bot/handlers/admin.py` - админ-панель
- ❌ `bot/handlers/settings.py` - настройки
- ❌ `bot/handlers/roles/` - вся папка с ролями
- ❌ `bot/keyboards/inline.py` - старые клавиатуры
- ❌ `bot/utils/sun_info.py` - расчет заката (для paintball)
- ❌ `run.py` - старый скрипт запуска

**Результат:** Проект теперь содержит только код для Lumi Beauty! ✨

---

**Обновлено:** 2025 | **Версия:** 2.0
