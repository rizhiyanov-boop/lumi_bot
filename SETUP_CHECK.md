# ✅ Проверка установки проекта

## 🔍 Автоматическая проверка

### Вариант 1: Через скрипт (рекомендуется)
```bash
check_setup.bat
```

### Вариант 2: Напрямую через Python
```bash
python check_dependencies.py
```

## 📋 Что проверяется

### 1. Python версия
- ✅ Требуется Python 3.10+
- ✅ Проверяется через `sys.version_info`

### 2. Обязательные модули
- ✅ `python-telegram-bot` (telegram)
- ✅ `sqlalchemy`
- ✅ `python-dotenv` (dotenv)
- ✅ `qrcode`
- ✅ `pillow` (PIL)

### 3. QR-код зависимости
- ✅ Модуль `qrcode`
- ✅ Константы `qrcode.constants`
- ✅ Модуль `PIL.Image`
- ✅ Модуль `io.BytesIO`
- ✅ Тестовая генерация QR-кода

### 4. Конфигурация
- ✅ Файл `.env` существует
- ✅ `BOT_TOKEN` установлен (обязательно)
- ✅ `CLIENT_BOT_TOKEN` установлен (обязательно)
- ✅ `CLIENT_BOT_USERNAME` (опционально, но нужно для QR)
- ✅ `DATABASE_URL` (опционально, по умолчанию SQLite)
- ✅ `SUPER_ADMINS` (опционально)

### 5. База данных
- ✅ Импорт модулей БД
- ✅ Создание/проверка таблиц

### 6. Дополнительные файлы
- ✅ `logo.png` (опционально - для QR-кода с логотипом)

---

## 🔧 Установка зависимостей

Если проверка показала ошибки, установите зависимости:

### Автоматически:
```bash
install_dependencies.bat
```

### Вручную:
```bash
pip install -r requirements.txt
```

Или:
```bash
pip install python-telegram-bot==22.5 sqlalchemy==2.0.44 python-dotenv==1.0.0 qrcode==8.2 pillow==12.0.0
```

---

## ❗ Решение проблем с QR-кодом

### Проблема: "Не установлены библиотеки qrcode/pillow"

**Решение:**
```bash
pip install qrcode pillow
```

### Проблема: "ImportError: No module named 'qrcode'"

**Решение:**
1. Убедитесь, что используете правильный Python:
   ```bash
   python --version
   ```
2. Установите через pip:
   ```bash
   python -m pip install qrcode pillow
   ```

### Проблема: QR-код генерируется, но не отправляется

**Возможные причины:**
- ❌ Ошибка в функции генерации
- ❌ Проблема с BytesIO
- ❌ Ошибка отправки фото в Telegram

**Проверка:**
1. Проверьте логи бота на наличие ошибок
2. Убедитесь, что бот имеет права на отправку фото
3. Проверьте размер QR-кода (может быть слишком большим)

---

## 📝 Обновленный код генерации QR

Код генерации QR был улучшен:
- ✅ Добавлена обработка ошибок
- ✅ Graceful fallback - если QR не сгенерируется, показывается ссылка
- ✅ Обработка отсутствия логотипа (работает без logo.png)
- ✅ Информативные сообщения об ошибках

---

## ✅ После проверки

Если все проверки прошли успешно:
1. ✅ Убедитесь, что `.env` настроен правильно
2. ✅ Запустите боты через `start_master.bat` и `start_client.bat`
3. ✅ Протестируйте генерацию QR-кода

---

**Дата создания:** 2025

