# Настройка оплаты ЮKassa

## Быстрый старт

1. **Получите секретный ключ из личного кабинета ЮKassa:**
   - Зарегистрируйтесь или войдите в [личный кабинет ЮKassa](https://yookassa.ru/)
   - Перейдите в раздел "Настройки" → "Ключи API"
   - Скопируйте **Секретный ключ** (Secret Key)

2. **Добавьте секретный ключ в `.env` файл:**
   ```env
   YOOKASSA_SHOP_ID=1200992
   YOOKASSA_SECRET_KEY=your_secret_key_here
   YOOKASSA_TEST_MODE=true
   PREMIUM_PRICE=299.00
   PREMIUM_DURATION_DAYS=30
   ```

3. **Установите зависимости:**
   ```bash
   pip install requests
   ```
   Или используйте `requirements.txt`:
   ```bash
   pip install -r requirements.txt
   ```

## Тестирование

Для тестирования используйте тестовую карту:
- **Номер карты:** `5555 5555 5555 4444`
- **Любой CVC код** (например: `123`)
- **Любая дата в будущем** (например: `12/25`)

## Функционал

### Премиум подписка

Мастера могут:
1. Перейти в **Настройки** → **⭐ Премиум**
2. Нажать **💳 Купить премиум**
3. Перейти по ссылке для оплаты
4. После оплаты подписка активируется автоматически

### Обработка платежей

- Платежи создаются через API ЮKassa
- Статус проверяется автоматически при возврате пользователя
- Подписка активируется автоматически после успешной оплаты
- Можно проверить статус вручную через кнопку "🔄 Проверить статус"

## Важно

⚠️ **Для продакшена:**
- Замените тестовый `YOOKASSA_SHOP_ID` на реальный
- Установите `YOOKASSA_TEST_MODE=false`
- Убедитесь, что секретный ключ соответствует продакшен магазину

## Документация

- [Быстрый старт ЮKassa](https://yookassa.ru/developers/payment-acceptance/getting-started/quick-start)
- [API ЮKassa](https://yookassa.ru/developers/api)

