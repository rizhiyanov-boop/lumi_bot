# 🚀 Быстрая сборка APK для тестирования

## Вариант 1: Через Android Studio (Рекомендуется)

1. **Откройте Android Studio**
2. **File → Open** → выберите папку `mobile_app/android/`
3. Дождитесь синхронизации Gradle (может занять несколько минут при первом запуске)
4. **Build → Build Bundle(s) / APK(s) → Build APK(s)**
5. После сборки нажмите **locate** или найдите APK в:
   ```
   mobile_app/android/app/build/outputs/apk/debug/app-debug.apk
   ```

## Вариант 2: Через командную строку

### Windows:
```powershell
cd mobile_app\android
.\gradlew.bat assembleDebug
```

### Linux/Mac:
```bash
cd mobile_app/android
./gradlew assembleDebug
```

APK будет в: `mobile_app/android/app/build/outputs/apk/debug/app-debug.apk`

## ⚠️ Важно перед сборкой:

1. **Установите Android SDK** (через Android Studio SDK Manager)
2. **Настройте API endpoint** в `mobile_app/android/app/src/main/java/com/lumi/beauty/di/AppModule.kt`:
   - Для эмулятора: `http://10.0.2.2:8000/`
   - Для реального устройства: `http://YOUR_IP:8000/` (узнайте IP через `ipconfig`)

3. **Запустите REST API**:
   ```bash
   cd mobile_app/api
   python run_api.py
   ```

## 📱 Установка APK на устройство

### Через ADB:
```bash
adb install app-debug.apk
```

### Или просто:
- Скопируйте APK на телефон
- Откройте файл на устройстве
- Разрешите установку из неизвестных источников (если нужно)

## 🔧 Если сборка не работает:

1. Убедитесь, что Android Studio установлен
2. Откройте проект в Android Studio хотя бы раз для генерации Gradle wrapper
3. Проверьте, что JDK 17+ установлен
4. Убедитесь, что интернет подключен (для загрузки зависимостей)

## 📝 Примечание

Для первого запуска Gradle загрузит все зависимости (может занять 5-10 минут).
Последующие сборки будут быстрее.

